# Duat Report — 2026-05-10 10:55

## Run Metadata
- Flags: `--harden` (full review + autonomous fix loop), `--include-tests`, `--shard-size 500000`
- Scope: `.` (whole repo)
- Packed: 241 files in 4 shards (~500K tokens each)
- Languages: C# 201, PowerShell 8, C++ 1, other 31
- Suggested focus (not applied): low-latency, media
- Gates: 7/7 complete
- Findings before Gate 6: ~480 raw
- After Gate 6 verdicts: ~339 confirmed (across all 4 shards), ~93 merged as duplicates, 3 dropped (false positives), 13 unresolved

## Summary
- 96 ranked findings (after dedup, merge, and root-cause clustering)
- 8 P0, 21 P1, 19 P2, 30 P3, 18 P4
- P0+P1 diffs written: 14 (covers the 8 P0 and 6 P1 root-cause entries that admit a localized, mechanical fix; the remaining P1 entries describe diff sketches in prose because they require coordinated changes across multiple subsystems and exceed a single-hunk diff)
- 13 unresolved items listed in `## Unresolved`
- Harden status: pending (this report drives the harden loop; the loop will commit fixes per `Iron Laws` and re-emit this file when complete)

## Findings (ranked)

### Recording integrity & data loss

#### #1 — P0 — `ReinitializeDeviceAsync` tears down the capture pipeline mid-recording (no `IsRecording` guard)
**Files:** `Sussudio/ViewModels/MainViewModel.Capture.cs:431-505`
**Surfaced by:** Gate 4 #2 (shard 2), Gate 2 §rapid-settings cluster, Gate 3 "Rapid sequential setting changes"
**What breaks:** The reinitialize path is reachable through any device-format / codec / quality cycle. Between line 504 (`StopPreviewAsync(teardownPipeline: true)`) and line 510 (`InitializeDeviceAsync`), the entire capture pipeline is torn down and rebuilt. There is **no guard** against entry while `IsRecording == true`. A user (or automation harness) toggling a property the UI binds to `ReinitializeDeviceAsync` will end an in-flight recording with whatever moov-state the encoder happened to be in. The flashback buffer is purged as part of the teardown. UI does not refuse the change; the recording timer stops as a side effect.
**Scenario:** Operator running an 8-hour stream changes resolution from 1080p60 to 1080p30 via UI — recording is silently truncated and flashback buffer purged. No confirmation prompt.
**Suggested fix:**
```diff
--- a/Sussudio/ViewModels/MainViewModel.Capture.cs
+++ b/Sussudio/ViewModels/MainViewModel.Capture.cs
@@ -431,6 +431,16 @@
     private async Task ReinitializeDeviceAsync(string reason)
     {
         if (SelectedDevice == null || SelectedFormat == null)
             return;
 
+        // Reinit tears down the capture pipeline (StopPreviewAsync teardownPipeline:true
+        // at line 504 below) and rebuilds it. While recording is in flight that is a
+        // data-loss event: the encoder is dropped mid-stream, the flashback buffer is
+        // purged, and the resulting MP4 is missing its moov atom. Reject the call here
+        // and let the caller surface the reason to the user.
+        if (IsRecording)
+        {
+            Logger.Log($"REINIT_REJECTED reason='{reason}' cause=recording_active");
+            StatusText = "Cannot change capture settings while recording. Stop recording first.";
+            return;
+        }
+
         var reinitializeGeneration = Interlocked.Increment(ref _previewReinitializeGeneration);
         await Task.Delay(PreviewReinitializeDebounceMs).ConfigureAwait(true);
```

#### #2 — P0 — No single-instance guard; second Sussudio instance cross-deletes the first instance's flashback segments
**Files:** `Sussudio/App.xaml.cs:124-160`, `Sussudio/Services/Automation/NamedPipeAutomationServer.cs:452-460`
**Surfaced by:** Gate 4 #8 (shard 2), Gate 3 "Two windows / two instances", Gate 3 "Concurrent instance pointed at .git/objects"
**What breaks:** `App.OnLaunched` happily wires a second `MainWindow`. `NamedPipeAutomationServer.CreateDefaultServerStream` uses `NamedPipeServerStream.MaxAllowedServerInstances`, so two instances both bind the pipe. Both instances enumerate the same temp directory (`%TEMP%\Sussudio`) on startup; the stale-session cleanup heuristic deletes any 32-hex empty directory it does not recognize as marked. The second instance's cleanup pass therefore deletes the first instance's *active* flashback segments. Settings are also clobbered last-writer-wins.
**Scenario:** User double-clicks the app while a recording is mid-flight from a forgotten earlier launch. The second instance's startup cleanup deletes the first instance's flashback segment files; the first instance's exporter fails on its next segment read.
**Suggested fix:**
```diff
--- a/Sussudio/App.xaml.cs
+++ b/Sussudio/App.xaml.cs
@@ -1,5 +1,6 @@
 using System;
 using System.Diagnostics;
+using System.Threading;
 using System.IO;
 using System.Reflection;
 using Microsoft.UI.Xaml;
@@ -19,6 +20,8 @@ namespace Sussudio
     public partial class App : Application
     {
         private Window? _window;
+        private static Mutex? _singleInstanceMutex;
+        private const string SingleInstanceMutexName = @"Local\Sussudio.SingleInstance.v1";
 
         public App()
         {
@@ -124,6 +127,17 @@ namespace Sussudio
 
         protected override void OnLaunched(Microsoft.UI.Xaml.LaunchActivatedEventArgs args)
         {
+            // Refuse to launch a second instance — the named pipe, settings file,
+            // and flashback temp-session cleanup heuristic are all single-writer
+            // resources. A second instance's startup cleanup deletes the first
+            // instance's active flashback segments. See Duat finding #2.
+            _singleInstanceMutex = new Mutex(initiallyOwned: true, name: SingleInstanceMutexName, out var createdNew);
+            if (!createdNew)
+            {
+                Logger.LogFatalBreadcrumb("APP_SECOND_INSTANCE_REFUSED");
+                Environment.Exit(0);
+                return;
+            }
+
             try
             {
                 var exePath = Environment.ProcessPath ?? Process.GetCurrentProcess().MainModule?.FileName ?? "unknown";
```

#### #3 — P0 — `FlashbackExporter.AtomicMoveTempFile` silently overwrites the destination file
**Files:** `Sussudio/Services/Flashback/FlashbackExporter.cs:1803-1811`
**Surfaced by:** Gate 4 #3 (shard 1), Gate 4 #5 (shard 2), Gate 2 §498
**What breaks:** Export uses `File.Move(tmpPath, outputPath, overwrite: true)`. The catalog-side `output_path` validation does not enforce existence; an automation harness that re-uses the same `output_path` will silently destroy the previous export's MP4. There is no prompt, no rename-on-collision, no diagnostic in the success envelope.
**Scenario:** Operator triggers `flashback_export` via MCP with the same `output_path` they used 30 seconds ago to compare two takes — the first take is gone.
**Suggested fix:**
```diff
--- a/Sussudio/Services/Flashback/FlashbackExporter.cs
+++ b/Sussudio/Services/Flashback/FlashbackExporter.cs
@@ -1803,11 +1803,30 @@
-    private static void AtomicMoveTempFile(string tmpPath, string outputPath)
+    private static void AtomicMoveTempFile(string tmpPath, string outputPath, bool allowOverwrite)
     {
         if (!File.Exists(tmpPath))
         {
             throw new IOException($"Temporary export file was not created: '{tmpPath}'.");
         }
 
-        File.Move(tmpPath, outputPath, overwrite: true);
+        if (File.Exists(outputPath))
+        {
+            if (!allowOverwrite)
+            {
+                DeleteTempFileIfPresent(tmpPath);
+                throw new IOException(
+                    $"Flashback export refused: destination already exists and overwrite was not requested: '{outputPath}'.");
+            }
+            Logger.Log($"FLASHBACK_EXPORT_OVERWRITE path='{outputPath}'");
+        }
+
+        File.Move(tmpPath, outputPath, overwrite: allowOverwrite);
     }
```
Plus a call-site update at line 1841 to thread an `allowOverwrite` boolean defaulted from the export request (`Force` flag on the automation envelope). The automation contract for `flashback_export` gains a new optional `force` bool that the operator must explicitly set; default behavior becomes refuse-on-exists.

#### #4 — P0 — `capture-rewrite-live-baseline.ps1` cleanup destroys the user's flashback buffer
**Files:** `tools/capture-rewrite-live-baseline.ps1:946-989`
**Surfaced by:** Gate 2 §Baseline cleanup-order (shard 4), Gate 3 §E "Concurrent verify+baseline destroys Flashback"
**What breaks:** The cleanup orders `SetRecordingEnabled false` then `SetFlashbackEnabled <initialFlashback>`. If the operator started the baseline mid-flashback-recording, `SetRecordingEnabled false` first finalizes the libav-backed recording **but the flashback backend is still wired**; then `SetFlashbackEnabled` toggles flashback off-then-on for the restore, which **purges all rolling segments**. The script silently destroys the user's flashback buffer state. The script also calls `Restore-AppState` from a top-level finally, so any scenario throw skips later scenarios but still runs cleanup with stale `$script:InitialSnapshot`.
**Scenario:** Operator runs `capture-rewrite-live-baseline.ps1` against a running production app with 30 minutes of flashback buffered. On normal exit, the script restores `FlashbackActive=true`. On the way there, segments are purged.
**Suggested fix:**
```diff
--- a/tools/capture-rewrite-live-baseline.ps1
+++ b/tools/capture-rewrite-live-baseline.ps1
@@ -946,6 +946,18 @@ function Restore-AppState {
     if ($null -eq $script:InitialSnapshot) {
         return
     }
 
+    # Refuse to mutate flashback state if the operator started us against an
+    # app that already had a flashback buffer. Toggling SetFlashbackEnabled
+    # off-and-back-on purges segments — that is a data-loss event.
+    $initialFlashback = Convert-ToBool (Get-PropertyValue $script:InitialSnapshot "FlashbackActive" $false)
+    $currentSnapshot = $null
+    try { $currentSnapshot = Get-Snapshot } catch { $currentSnapshot = $null }
+    $currentFlashback = Convert-ToBool (Get-PropertyValue $currentSnapshot "FlashbackActive" $false)
+    $flashbackChanged = ($null -ne $currentSnapshot) -and ($initialFlashback -ne $currentFlashback)
+
     try {
         $current = Get-Snapshot
         if (Convert-ToBool (Get-PropertyValue $current "IsRecording" $false)) {
@@ -971,12 +983,16 @@ function Restore-AppState {
         return
     }
 
-    try {
-        $initialFlashback = Convert-ToBool (Get-PropertyValue $script:InitialSnapshot "FlashbackActive" $false)
-        Invoke-Automation -Command "SetFlashbackEnabled" -Payload @{ enabled = $initialFlashback } -AllowFailure | Out-Null
-    }
-    catch {
-        Write-Warning "Flashback restore failed: $($_.Exception.Message)"
+    if ($flashbackChanged) {
+        try {
+            Invoke-Automation -Command "SetFlashbackEnabled" -Payload @{ enabled = $initialFlashback } -AllowFailure | Out-Null
+        }
+        catch {
+            Write-Warning "Flashback restore failed: $($_.Exception.Message)"
+        }
+    }
+    else {
+        Write-Host "Restore-AppState: skipping SetFlashbackEnabled — state unchanged (flashback=$initialFlashback). Toggle would purge segments."
     }
```

#### #5 — P0 — `AutomationCommandKind` numeric IDs reorder on edit; stale ssctl/MCP/StreamDeck misroute commands
**Files:** `Sussudio/Models/AutomationCommandKind.cs:5-50+`, also linked into `tools/ssctl/ssctl.csproj`, `tools/McpServer/McpServer.csproj`, `tools/AutomationClient/AutomationClient.csproj`, `tools/Common/AutomationPipeProtocol.cs`
**Surfaced by:** Gate 2 cross-cutting #7 (shard 4), Gate 3 §E "AutomationCommandKind numeric ID drift", Gate 3 §B "Stale binaries on disk"
**What breaks:** The enum is `public enum AutomationCommandKind { Authenticate = 0, GetSnapshot = 1, ... }` — values *are* explicit today, but the file is `Compile Include="..\..\Sussudio\Models\AutomationCommandKind.cs"` from three external `.csproj` files. The convention is "preserve existing values," but nothing in the build prevents a reordering edit. The pipe protocol serializes the numeric kind, not the name; a stale ssctl.dll bundled with an older Sussudio.exe will silently invoke the wrong command (e.g., StreamDeck plugin sends `StopRecording` and gets `RestartFlashback`).
**Scenario:** Developer inserts a new enum member in the middle. CI builds Sussudio.exe fine; old ssctl binary on a tester's machine routes `SetRecordingFormat` (was 9) to `SetQuality` (now 9). Silent data corruption in the recording profile.
**Suggested fix:** Add a build-time guard that hashes the enum (name + value pairs in declared order) and writes it as a `[GeneratedRuntimeIdentifier]` on the assembly; both server and client verify they match on the first command exchange. Mechanical version:
```diff
--- a/Sussudio/Models/AutomationCommandKind.cs
+++ b/Sussudio/Models/AutomationCommandKind.cs
@@ -1,6 +1,12 @@
 namespace Sussudio.Models;
 
-// Numeric automation command identifiers shared by the app, ssctl, MCP, and the
-// generic AutomationClient. Preserve existing values for protocol compatibility.
+// Numeric automation command identifiers shared by the app, ssctl, MCP, and the
+// generic AutomationClient. PRESERVE EXISTING VALUES for protocol compatibility.
+//
+// IMPORTANT: When adding a new member, append it at the END with the next free
+// value and bump AutomationPipeProtocol.CommandManifestRevision. The pipe
+// server rejects connections whose first envelope reports a different manifest
+// revision; this stops a stale client from binding numbers to the wrong names.
+// See AutomationPipeProtocol.cs.
 public enum AutomationCommandKind
 {
```
And in `tools/Common/AutomationPipeProtocol.cs` add a `CommandManifestRevision` const (start at `1`) plus a handshake check in the first envelope; mismatched revisions fail with `status="manifest-mismatch"`. Sketch in prose because the full handshake change touches Authenticate logic in `AutomationCommandDispatcher`.

#### #6 — P0 — Operator Ctrl-C in ssctl / baseline / verify scripts silently leaves the app recording
**Files:** `tools/ssctl/Program.cs`, `tools/AutomationClient/Program.cs`, `tools/capture-rewrite-live-baseline.ps1`, `tools/verify-dedicated-libav-recording.ps1`, `tools/automation-snapshot-smoke.ps1`
**Surfaced by:** Gate 3 §A "Operator Ctrl-C silently leaves recording" (shard 4)
**What breaks:** No `Console.CancelKeyPress` handler is registered anywhere in the tools tree (grep returned zero hits). A long-running scenario that calls `SetRecordingEnabled true` and Ctrl-C's during the workload leaves the recording active in the app, with no cleanup. The PowerShell harnesses use top-level try/finally but the `Stop-Process` for any child `ssctl.exe` process is also missing.
**Scenario:** CI job hits its timeout, sends SIGTERM; recording continues until the next manual intervention or disk fills.
**Suggested fix:**
```diff
--- a/tools/ssctl/Program.cs
+++ b/tools/ssctl/Program.cs
@@ -1,3 +1,4 @@
+using System;
 using System.Threading;
 using System.Threading.Tasks;
 
@@ -10,7 +11,16 @@ internal static class Program
 {
     internal static async Task<int> Main(string[] args)
     {
-        var cts = new CancellationTokenSource();
+        using var cts = new CancellationTokenSource();
+        Console.CancelKeyPress += (_, e) =>
+        {
+            e.Cancel = true;
+            if (!cts.IsCancellationRequested)
+            {
+                Console.Error.WriteLine("ssctl: Ctrl-C received; cancelling and emitting cleanup hint.");
+                cts.Cancel();
+            }
+        };
         try
         {
             return await CommandHandlers.RunAsync(args, cts.Token).ConfigureAwait(false);
```
Plus a per-script `trap { ... } 'CtrlC'` in the PowerShell harnesses that calls `Restore-AppState` before exiting.

#### #7 — P0 — Diagnostic-session concurrent runs corrupt artifacts (no mutex between operator tools)
**Files:** `tools/Common/DiagnosticSessionRunner.cs`, `tools/ssctl/CommandHandlers.cs`, `tools/McpServer/Tools/DiagnosticSessionTools.cs`, `tools/capture-rewrite-live-baseline.ps1`, `tools/verify-dedicated-libav-recording.ps1`, `tools/reliability-gates.ps1`
**Surfaced by:** Gate 2 cross-cutting #5 (shard 4), Gate 2 §Diagnostic-session concurrent (shard 4), Gate 3 §A "Concurrent CI runners corrupt artifacts"
**What breaks:** Two `ssctl diagnostic-session …` invocations to the same `OutputDirectory` are unmutexed. They write `manifest.json`, `final.snapshot.json`, and the per-scenario JSON files concurrently. Last-writer-wins for the manifest; partial JSON for the per-scenario files. CI matrix runs that parallelize Sussudio variants on one machine hit this on every run.
**Scenario:** Two parallel CI matrix jobs share `D:\runners\artifacts` — the manifest from job A is overwritten by job B's partial mid-run state, then job A finishes and overwrites it again. The on-disk artifact set is internally inconsistent.
**Suggested fix:** Add a per-output-directory `FileStream` lock acquired before any artifact write. Sketch:
```diff
--- a/tools/Common/DiagnosticSessionRunner.cs
+++ b/tools/Common/DiagnosticSessionRunner.cs
@@ -1,5 +1,6 @@
 using System;
 using System.IO;
+using System.Threading;
 using System.Threading.Tasks;
 
 namespace Sussudio.Tools.Common;
@@ -20,6 +21,21 @@ public static class DiagnosticSessionRunner
     {
         Directory.CreateDirectory(options.OutputDirectory);
+        var lockPath = Path.Combine(options.OutputDirectory, ".sussudio-diag.lock");
+        FileStream? lockHandle = null;
+        try
+        {
+            lockHandle = new FileStream(
+                lockPath,
+                FileMode.OpenOrCreate,
+                FileAccess.Write,
+                FileShare.None,    // exclusive — second instance throws
+                bufferSize: 1,
+                FileOptions.DeleteOnClose);
+        }
+        catch (IOException ex)
+        {
+            throw new InvalidOperationException(
+                $"Another diagnostic session is already running in '{options.OutputDirectory}'. "
+                + $"Wait for it to finish or choose a different output directory. ({ex.Message})",
+                ex);
+        }
         try
         {
             return await RunCoreAsync(options, cancellationToken).ConfigureAwait(false);
         }
+        finally
+        {
+            lockHandle?.Dispose();
+        }
     }
```

#### #8 — P0 — `IsRecoverableUnhandled` only allows `OperationCanceledException`; any other background exception triggers `FailFast` mid-recording
**Files:** `Sussudio/App.xaml.cs:35-38`, `Sussudio/App.xaml.cs:78-84`
**Surfaced by:** Gate 2 §353, Gate 3 "OOM in capture callback propagates through MF → FailFast"
**What breaks:** The classifier is one line: `return ex is OperationCanceledException;`. Any other exception that surfaces to `App_UnhandledException` or `CurrentDomain_UnhandledException` triggers a 3-second emergency stop followed by `Environment.FailFast`. The 3s budget is below the 10s flashback-exporter Dispose budget and the 30s libav-encoder StopAsync budget (see #10). Common background-thread exceptions in a media pipeline (`MFE_NOTACCEPTING`, `AUDCLNT_E_DEVICE_INVALIDATED`, DXGI device-removed, temporary IO faults on a flaky USB capture device) escalate straight to process kill with a truncated MP4. The user sees the app vanish.
**Scenario:** Capture device USB port flaps for 50ms during an 8h stream → MF surfaces an HRESULT on the worker pool → `FailFast` → moov atom missing.
**Suggested fix:**
```diff
--- a/Sussudio/App.xaml.cs
+++ b/Sussudio/App.xaml.cs
@@ -33,9 +33,29 @@ namespace Sussudio
         }
 
-        private static bool IsRecoverableUnhandled(Exception ex)
+        private static bool IsRecoverableUnhandled(Exception ex)
         {
-            return ex is OperationCanceledException;
+            // Broaden the recovery classifier beyond OperationCanceledException.
+            // A media app routinely sees transient HRESULT-wrapped exceptions on
+            // background MF/WASAPI/D3D11 worker threads. Killing the process for
+            // every one of those is worse than logging, surfacing to the user,
+            // and letting the next frame retry.
+            if (ex is OperationCanceledException) return true;
+            if (ex is System.IO.IOException) return true;            // transient file IO
+            if (ex is TimeoutException) return true;
+            if (ex is System.Runtime.InteropServices.COMException com)
+            {
+                // Device-removed / format-changed / not-accepting — recoverable
+                // by the producer's existing retry path.
+                unchecked
+                {
+                    return com.HResult == (int)0x887A0005   // DXGI_ERROR_DEVICE_REMOVED
+                        || com.HResult == (int)0x88890004   // AUDCLNT_E_DEVICE_INVALIDATED
+                        || com.HResult == (int)0xC00D36B5   // MF_E_NOTACCEPTING
+                        || com.HResult == (int)0xC00D4A44;  // MF_E_INVALID_STREAM_DATA
+                }
+            }
+            return false;
         }
```

### Capture pipeline & encoder

#### #9 — P1 — `CaptureService.UpdateEncodingSettings` mutates session state without holding the transition lock (root cause for the rapid-settings cluster)
**Files:** `Sussudio/Services/Capture/CaptureService.cs:335-352`
**Surfaced by:** Gate 4 #1 (shard 1), Gate 2 §551, Gate 2 §553, Gate 3 "Rapid sequential settings"
**What breaks:** `UpdateEncodingSettings` writes ten fields of `_currentSettings` and sets `_pendingFlashbackSettingsChange` without acquiring `_sessionState`'s transition gate. Concurrent calls from `RestartFlashbackAsync(settings)` (line 382, which IS inside `RunTransitionAsync`) and *any* other path that calls `UpdateEncodingSettings` directly will interleave. A rapid sequence of UI toggles (`SetQuality`, `SetCustomBitrate`, `SetMicrophoneEnabled` chained in <500ms) tears the settings object and the subsequent flashback restart picks up a hybrid configuration. Each restart purges segments, so the user-observed effect is data loss with no warning.
**Suggested fix:**
```diff
--- a/Sussudio/Services/Capture/CaptureService.cs
+++ b/Sussudio/Services/Capture/CaptureService.cs
@@ -330,23 +330,33 @@
     /// <summary>
     /// Updates encoding-related fields in the active capture settings so that
     /// <see cref="RestartFlashbackAsync"/> picks up the latest bitrate/quality/preset.
     /// </summary>
-    // REVIEWED 2026-04-07: same threading rationale as UpdateFlashbackSettings above.
     public void UpdateEncodingSettings(CaptureSettings source)
     {
-        if (_currentSettings == null) return;
-        _currentSettings.Format = source.Format;
-        _currentSettings.Quality = source.Quality;
-        _currentSettings.NvencPreset = source.NvencPreset;
-        _currentSettings.CustomBitrateMbps = source.CustomBitrateMbps;
-        _currentSettings.AudioEnabled = source.AudioEnabled;
-        _currentSettings.MicrophoneEnabled = source.MicrophoneEnabled;
-        _currentSettings.MicrophoneDeviceId = source.MicrophoneDeviceId;
-        _currentSettings.MicrophoneDeviceName = source.MicrophoneDeviceName;
-        _currentSettings.FlashbackBufferMinutes = source.FlashbackBufferMinutes;
-        _currentSettings.FlashbackGpuDecode = source.FlashbackGpuDecode;
-        if (_isRecording && IsFlashbackRecordingBackendActive())
-            _pendingFlashbackSettingsChange = true;
+        // Must hold the same lock the transition pipeline acquires. Without it,
+        // a rapid sequence of UI toggles (SetQuality + SetCustomBitrate + …) tears
+        // _currentSettings between RestartFlashbackAsync's snapshot and its
+        // EnsureFlashbackPreviewBackendAsync call — the encoder rebuilds with a
+        // hybrid configuration and the next segment rotation may purge segments
+        // matched against the OLD config. (Gate 4 #1, root cause of rapid-settings.)
+        lock (_sessionState.SyncRoot)
+        {
+            if (_currentSettings == null) return;
+            _currentSettings.Format = source.Format;
+            _currentSettings.Quality = source.Quality;
+            _currentSettings.NvencPreset = source.NvencPreset;
+            _currentSettings.CustomBitrateMbps = source.CustomBitrateMbps;
+            _currentSettings.AudioEnabled = source.AudioEnabled;
+            _currentSettings.MicrophoneEnabled = source.MicrophoneEnabled;
+            _currentSettings.MicrophoneDeviceId = source.MicrophoneDeviceId;
+            _currentSettings.MicrophoneDeviceName = source.MicrophoneDeviceName;
+            _currentSettings.FlashbackBufferMinutes = source.FlashbackBufferMinutes;
+            _currentSettings.FlashbackGpuDecode = source.FlashbackGpuDecode;
+            if (_isRecording && IsFlashbackRecordingBackendActive())
+                _pendingFlashbackSettingsChange = true;
+        }
     }
```
(Adjust to whatever the actual sync primitive is on `_sessionState`. If `_sessionState` exposes `SyncRoot` as the existing transition lock object, this is the right form. Otherwise the helper that wraps `RunTransitionAsync` should accept a synchronous fast-path that takes the same gate.)

#### #10 — P1 — Mic-toggle-during-recording updates UI but skips device wiring
**Files:** `Sussudio/ViewModels/MainViewModel.Automation.cs:1265-1299`
**Surfaced by:** Gate 4 #1 (shard 2)
**What breaks:** When `request.IsRecording == true`, the `_sessionCoordinator.UpdateMicrophoneMonitorAsync` call is skipped (line 1274), but `IsMicrophoneEnabled = enabled` is still set unconditionally (line 1289). The user sees the mic toggle in the UI flip; the actual microphone is *not* rewired in the encoder; subsequent listeners read a UI state that doesn't match the device state.
**Suggested fix:**
```diff
--- a/Sussudio/ViewModels/MainViewModel.Automation.cs
+++ b/Sussudio/ViewModels/MainViewModel.Automation.cs
@@ -1273,15 +1273,28 @@ private async Task SetMicrophoneEnabledAutomationAsync(bool enabled, Cancellatio
 
-        if (!request.IsRecording)
-        {
-            await _sessionCoordinator.UpdateMicrophoneMonitorAsync(
-                enabled,
-                request.DeviceId,
-                request.DeviceName,
-                cancellationToken).ConfigureAwait(false);
-        }
+        if (request.IsRecording)
+        {
+            // Refuse silent toggles while recording — the device wiring path
+            // (UpdateMicrophoneMonitorAsync) intentionally cannot run mid-recording,
+            // but updating IsMicrophoneEnabled in the UI would lie about device state.
+            // The session coordinator owns the during-recording change set; either it
+            // grows a mid-recording mic-wiring transition or the call is refused.
+            Logger.Log($"MIC_TOGGLE_REFUSED reason=recording_active requested={enabled}");
+            throw new InvalidOperationException(
+                "Cannot change microphone enable state while recording. Stop the recording first.");
+        }
+
+        await _sessionCoordinator.UpdateMicrophoneMonitorAsync(
+            enabled,
+            request.DeviceId,
+            request.DeviceName,
+            cancellationToken).ConfigureAwait(false);
 
         await InvokeOnUiThreadAsync(
             () =>
```

#### #11 — P1 — `LibAvRecordingSink.StopAsync` returns Failure on encode-drain timeout but leaves the writer un-flushed; UI says "Recording saved", file has no moov atom
**Files:** `Sussudio/Services/Recording/LibAvRecordingSink.cs:508-590`
**Surfaced by:** Gate 4 #9 (shard 2), Gate 4 #10 cluster (shard 1), Gate 3 "TerminateProcess orphans MP4"
**What breaks:** When the encode task fails to drain inside `StopTimeoutMs`, the code (line 532) cancels `_cts` and *returns* without calling `_encoder.FlushAndClose()`. The mp4 on disk has no `moov` atom. The result envelope is a `FinalizeResult.Failure`, but multiple upstream paths (most notably the snapshot-builder and the per-tile UI "last recording saved" label) only look at `LastOutputPath`, not the success flag — so the user sees the path and assumes success until they try to play it.
**Suggested fix:**
```diff
--- a/Sussudio/Services/Recording/LibAvRecordingSink.cs
+++ b/Sussudio/Services/Recording/LibAvRecordingSink.cs
@@ -529,12 +529,21 @@
         if (_encodingTask != null)
         {
             var completedTask = await Task.WhenAny(_encodingTask, Task.Delay(StopTimeoutMs, cancellationToken)).ConfigureAwait(false);
             if (!ReferenceEquals(completedTask, _encodingTask))
             {
                 cancellationToken.ThrowIfCancellationRequested();
                 _cts?.Cancel();
-                return FinalizeResult.Failure(outputPath, "Stopped (libav encode drain timed out)");
+                // Best-effort: even if the encode task is still wedged, flushing the
+                // muxer writes whatever frames did make it through and emits the moov
+                // atom so the file is at least playable up to the truncation point.
+                // Without this, the on-disk file is unplayable (no moov) and the user
+                // sees "Recording saved at <path>" but the file is junk.
+                try { _encoder.FlushAndClose(); }
+                catch (Exception flushEx)
+                {
+                    Logger.Log($"LIBAV_SINK_STOP_DRAIN_FLUSH_FAIL type={flushEx.GetType().Name} msg={flushEx.Message}");
+                }
+                return FinalizeResult.Failure(outputPath, "Stopped (libav encode drain timed out; emergency flush attempted)");
             }
```

#### #12 — P1 — Emergency-stop budget cascade: App 3s ≪ FlashbackExporter Dispose 10s ≪ LibAv StopAsync 30s
**Files:** `Sussudio/App.xaml.cs:92-122`, `Sussudio/Services/Flashback/FlashbackExporter.cs` (Dispose path), `Sussudio/Services/Recording/LibAvRecordingSink.cs:508-547`
**Surfaced by:** Gate 4 #10 (shard 1), Gate 2 §512, Gate 5 #23, Gate 3 §SIGTERM/FailFast
**What breaks:** `App.TryEmergencyStopRecording` waits 3 seconds (`task.Wait(TimeSpan.FromSeconds(3))`); the downstream `LibAvRecordingSink.StopAsync` uses `StopTimeoutMs` which is far longer (the 30s budget in the comments); `FlashbackExporter.Dispose` documents a 10s budget. The 3s wrapper cancels before either of the downstream finalizers can finish. The outer log says `EMERGENCY_FINALIZE_TIMEOUT`, the file is truncated, the user sees nothing actionable.
**Suggested fix:** Either widen the wrapper to 10s and document the deliberate trade-off (3s was chosen because Windows Error Reporting starts its own timer at ~5s on a hang), or shorten `LibAvRecordingSink.StopTimeoutMs` and `FlashbackExporter.DisposeTimeoutMs` to fit inside the 3s wrapper. The fix is a coordinated three-file change — diff sketch only:

```diff
--- a/Sussudio/App.xaml.cs
+++ b/Sussudio/App.xaml.cs
@@ -88,7 +88,7 @@
-    // Best-effort: give the mux up to 3 seconds to write the moov atom before
-    // FailFast kills the process.
+    // Best-effort: 8 seconds — chosen to fit inside Windows Error Reporting's
+    // 10s hang heuristic but above the 5-second cliff. Downstream finalizers
+    // (LibAvRecordingSink.StopTimeoutMs and FlashbackExporter.DisposeTimeoutMs)
+    // are sized to fit under this ceiling. Do not raise individual budgets
+    // beyond this number without also raising it.
     private void TryEmergencyStopRecording(string source)
@@ -101,7 +101,7 @@
-                var finished = task.Wait(TimeSpan.FromSeconds(3));
+                var finished = task.Wait(TimeSpan.FromSeconds(8));
```
Plus `StopTimeoutMs = 5000;` (was longer) in `LibAvRecordingSink.cs` and `DisposeTimeoutMs = 5000;` in `FlashbackExporter.cs`.

#### #13 — P1 — `LibAvEncoder.InitializeFFmpeg` non-strict in `App` ctor; first export fails opaquely
**Files:** `Sussudio/App.xaml.cs:32`, `Sussudio/Services/Recording/LibAvEncoder.cs:217-266`
**Surfaced by:** Gate 2 §43, Gate 4 #16
**What breaks:** `App` ctor calls `LibAvEncoder.InitializeFFmpeg()` (line 32) without `requireNativeRuntime: true`. If the FFmpeg runtime isn't found, the method silently logs and returns. The app launches in a broken state. The first `FlashbackExport` or recording attempt fails with the generic message `Flashback export failed` because the codec lookup returns null, not a specific "FFmpeg runtime missing" diagnostic. Users perceive the bug as "export is broken," not "your FFmpeg deployment is broken."
**Suggested fix:**
```diff
--- a/Sussudio/App.xaml.cs
+++ b/Sussudio/App.xaml.cs
@@ -29,7 +29,18 @@ namespace Sussudio
             AppDomain.CurrentDomain.UnhandledException += CurrentDomain_UnhandledException;
 
             Logger.LogSystemInfo();
-            LibAvEncoder.InitializeFFmpeg();
+            try
+            {
+                LibAvEncoder.InitializeFFmpeg(requireNativeRuntime: true);
+            }
+            catch (Exception ex)
+            {
+                // Surface the missing runtime immediately rather than letting the
+                // first export fail with an opaque codec lookup error. The Logger
+                // call is also visible in the fatal breadcrumb.
+                Logger.LogFatalBreadcrumb("FFMPEG_RUNTIME_MISSING_AT_STARTUP", ex);
+                throw;
+            }
         }
```

#### #14 — P1 — `AutomationDiagnosticsHub.Dispose` synchronously waits on async work from any thread (UI-thread deadlock risk)
**Files:** `Sussudio/Services/Automation/AutomationDiagnosticsHub.cs:367-389`
**Surfaced by:** Gate 4 #8 (shard 1), Gate 2 §347, Gate 2 §692
**What breaks:** `Dispose()` calls `StopAsync().GetAwaiter().GetResult()`. The hub's refresh loop awaits things on captured contexts; if any awaiter dispatched back to the UI thread is still pending, and Dispose is called from the UI thread (which happens on `MainWindow.Closed`), the wait deadlocks until any internal timeout fires (or forever if there isn't one).
**Suggested fix:**
```diff
--- a/Sussudio/Services/Automation/AutomationDiagnosticsHub.cs
+++ b/Sussudio/Services/Automation/AutomationDiagnosticsHub.cs
@@ -365,15 +365,30 @@
-    public void Dispose()
+    public void Dispose()
     {
         if (_disposed)
         {
             return;
         }
 
         _disposed = true;
-        StopAsync().GetAwaiter().GetResult();
+        // Never block the UI thread on a Task that may itself need the UI thread
+        // to make progress (StopAsync awaits items that may have dispatched back).
+        // Use Task.Run to break the SynchronizationContext capture, with a hard
+        // budget. Callers that need deterministic teardown should call DisposeAsync.
+        try
+        {
+            var stop = Task.Run(() => StopAsync());
+            if (!stop.Wait(TimeSpan.FromSeconds(2)))
+            {
+                Logger.Log("DIAGHUB_DISPOSE_TIMEOUT msg='StopAsync did not complete within 2s; abandoning'");
+            }
+        }
+        catch (Exception ex)
+        {
+            Logger.Log($"DIAGHUB_DISPOSE_FAULT type={ex.GetType().Name} msg='{ex.Message}'");
+        }
+
         _currentProcess.Dispose();
     }
```

### Preview & GPU

#### #15 — P1 — GPU device-removed (DXGI_ERROR_DEVICE_REMOVED / TDR) silently purges the flashback buffer
**Files:** `Sussudio/Services/Preview/SharedD3DDeviceManager.cs`, `Sussudio/Services/Preview/D3D11PreviewRenderer.cs`, `Sussudio/Services/Capture/CaptureService.cs`
**Surfaced by:** Gate 3 "GPU device removed" (shard 1+2), Gate 3 "GPU TDR" (shard 2)
**What breaks:** Grep for `DEVICE_REMOVED` or `DXGI_ERROR_DEVICE_REMOVED` returns zero hits in the production C# tree. On a TDR (driver reset, common during driver updates and Optimus switches), the shared D3D11 device fails the next call; current code paths handle the resulting exception as a generic fatal-cleanup, which purges the flashback rolling buffer.
**Suggested fix:** Add a `Recover-or-purge` decision step in fatal-cleanup that distinguishes `DXGI_ERROR_DEVICE_REMOVED` (0x887A0005) from a fatal pipeline fault. On device-removed, reinitialize the shared device and re-attach the flashback writer **without** purging segments — the buffer is independent of the GPU. Diff sketch (skip — touches three files):

- Add an `IsDeviceRemoved(Exception)` predicate that walks COMException.HResult and `Marshal.GetHRForException`.
- In `CaptureService.HandleFatalCaptureError`, branch on device-removed → call `SharedD3DDeviceManager.TryRecoverAsync()` before deciding to purge.
- Add a `LastDeviceRecoveryAttemptUtc` field exposed via snapshot for automation telemetry.

#### #16 — P1 — Sleep/hibernate wake produces black preview and silent audio (no `PowerManager` handler; `AUDCLNT_E_DEVICE_INVALIDATED` unhandled)
**Files:** `Sussudio/MainWindow.xaml.cs`, `Sussudio/Services/Audio/WasapiAudioCapture.cs`, `Sussudio/Services/Capture/UnifiedVideoCapture.cs`
**Surfaced by:** Gate 3 "Machine wakes from sleep" (shard 1), Gate 3 "Sleep/hibernate wake" (shard 2)
**What breaks:** Grep returns zero hits for `PowerManager`. After resume, the MF source reader and WASAPI capture client both fail their next call with provider-specific HRESULTs; current code surfaces a generic `StatusText` error and does not auto-rebind. Users see a black preview window and have to manually re-pick the device.
**Suggested fix sketch:** Subscribe to `PowerManager.SuspendingOperation` / `PowerManager.Resuming` (WinAppSDK has equivalents via `Microsoft.Windows.System.Power`) and force a `ReinitializeDeviceAsync` on resume (with the new `IsRecording` guard from finding #1 making the rebuild safe). Add `AUDCLNT_E_DEVICE_INVALIDATED` (0x88890004) handling in `WasapiAudioCapture` to rebind to the same endpoint id. Multi-file; sketch only.

### Flashback DVR

#### #17 — P1 — `Disable flashback` while non-flashback recording is active purges segments
**Files:** `Sussudio/ViewModels/MainViewModel.cs` SetFlashbackEnabled path; `Sussudio/Services/Capture/CaptureService.cs` (BeginFlashbackBackendCleanup)
**Surfaced by:** Gate 4 #6 (shard 1), Gate 2 §390
**What breaks:** The `SetFlashbackEnabled false` automation command, invoked while a non-flashback (libav-direct) recording is running, calls into `BeginFlashbackBackendCleanup` which deletes the rolling buffer's segment files. From the user perspective, the recording is fine but their DVR buffer for the *current* session is gone. The catalog does not document that this is a destructive operation.
**Suggested fix sketch:** Refuse `SetFlashbackEnabled false` while any recording (flashback or libav) is active, with a clear error. Or: make the cleanup path defer segment purge until the recording finishes (requires storing the pending action). Diff omitted because the right call hinges on UX choice not visible from code; pin to the refuse-during-recording variant for hardening pass.

#### #18 — P1 — `CycleFlashbackBufferAsync` failure on settings change → buffer purged → live segments lost
**Files:** `Sussudio/Services/Capture/CaptureService.cs:387-403`
**Surfaced by:** Gate 2 §303
**What breaks:** `RestartFlashbackCoreAsync` always calls `DisposeFlashbackPreviewBackendAsync(..., purgeSegments: true)`. If `EnsureFlashbackPreviewBackendAsync` then throws (NVENC out-of-memory, codec init failure on a downgraded driver, etc.), the previous segments are gone *and* the new backend never starts. The user has neither the old DVR buffer nor a new one. Diff sketch — purge should be conditional on the new backend coming up successfully:

```diff
--- a/Sussudio/Services/Capture/CaptureService.cs
+++ b/Sussudio/Services/Capture/CaptureService.cs
@@ -387,9 +387,18 @@
     private async Task RestartFlashbackCoreAsync(CancellationToken cancellationToken)
     {
-        await DisposeFlashbackPreviewBackendAsync(cancellationToken, purgeSegments: true).ConfigureAwait(false);
+        // Tear down WITHOUT purging segments first — we only purge once the new
+        // backend successfully starts and we know the new encoder settings are
+        // incompatible with the old segments. If the new backend fails to come
+        // up, the user keeps their buffered video.
+        await DisposeFlashbackPreviewBackendAsync(cancellationToken, purgeSegments: false).ConfigureAwait(false);
 
         var committedRestartToken = CancellationToken.None;
         var unifiedVideoCapture = _unifiedVideoCapture;
         var settings = _currentSettings;
         if (!_flashbackEnabled || unifiedVideoCapture == null || settings == null)
         {
             Logger.Log($"FLASHBACK_RESTART_TEARDOWN_ONLY enabled={_flashbackEnabled} capture={unifiedVideoCapture != null} settings={settings != null}");
             return;
         }
 
         await EnsureFlashbackPreviewBackendAsync(unifiedVideoCapture, settings, committedRestartToken).ConfigureAwait(false);
+        // Only purge the previous-format segments once we have a working new backend.
+        await PurgeIncompatibleFlashbackSegmentsAsync(cancellationToken).ConfigureAwait(false);
         Logger.Log("FLASHBACK_RESTART_OK");
         cancellationToken.ThrowIfCancellationRequested();
     }
```
(Implementation note: `PurgeIncompatibleFlashbackSegmentsAsync` is new and uses the same scan path `DisposeFlashbackPreviewBackendAsync(purgeSegments: true)` did, factored out.)

### Automation, MCP, tooling

#### #19 — P1 — Synthetic-error envelope shape diverges between ssctl (throws) and MCP (Success=false) — operators get inconsistent cleanup paths
**Files:** `tools/ssctl/PipeTransport.cs`, `tools/McpServer/PipeClient.cs`
**Surfaced by:** Gate 2 cross-cutting #1 (shard 4), Gate 2 §Diagnostic-session, Gate 2 §Verify
**What breaks:** When the pipe fails (connect timeout, malformed response), ssctl throws `IOException`; MCP synthesizes a `{Success:false, Status:"transport-error", …}` envelope. Operators writing dual-tool scripts have to handle both shapes. The Gate 2 verdict pins this as the root cause of multiple downstream confusions ("ssctl exit 3 vs pipe-protocol error" #3-shard4-Gate2). Fix is to make ssctl emit the same synthetic-failure envelope structure on the wire (printing a JSON object even on transport failure), and document it. Diff sketch only (touches two CLI surfaces; needs aligned manifest).

#### #20 — P1 — Snapshot field-name drift between server (`CurrentRecordingPath`) and client formatters (`OutputPath` etc.)
**Files:** `tools/Common/AutomationSnapshotFormatter.cs`, `tools/ssctl/Formatters.cs`, `Sussudio/Models/AutomationContracts.cs`
**Surfaced by:** Gate 2 cross-cutting #2 (shard 4), Gate 5 #15 (shard 3 locale duplication)
**What breaks:** Two reader paths use different field name conventions; renames in `Sussudio` silently print `N/A` in tools. Fix is a shared source-generated contract assembly; sketch.

#### #21 — P1 — AuthToken inherited into Sussudio.exe child process env block, leaking the token to descendants
**Files:** `tools/capture-rewrite-live-baseline.ps1:301`, `tools/verify-dedicated-libav-recording.ps1`, similar in `automation-snapshot-smoke.ps1`
**Surfaced by:** Gate 2 cross-cutting #3 (shard 4), Gate 3 §E "SUSSUDIO_AUTOMATION_TOKEN env leak"
**What breaks:** Tools that launch Sussudio.exe propagate `SUSSUDIO_AUTOMATION_TOKEN` via the env block. Anything Sussudio spawns inherits it. Local-only attack surface but real for shared CI runners.
**Suggested fix:**
```diff
--- a/tools/capture-rewrite-live-baseline.ps1
+++ b/tools/capture-rewrite-live-baseline.ps1
@@ -298,7 +298,11 @@
-    $startInfo.EnvironmentVariables["SUSSUDIO_AUTOMATION_TOKEN"] = $AuthToken
+    # Do NOT propagate the auth token via env block — Sussudio reads its token
+    # from the pipe handshake. Inheriting it leaks the token to every grandchild
+    # process (logger, ffprobe, etc.). The token MUST be passed only on the
+    # pipe wire from the launching tool.
+    # (Intentionally omitted: SUSSUDIO_AUTOMATION_TOKEN env propagation.)
     # ... continue env block setup ...
```

#### #22 — P1 — Tests assert against source text (grep tests) — locks in formatting and produces false-positive/false-negative regressions
**Files:** `tests/Sussudio.Tests/Flashback.Tests.cs:198-219`, `tests/Sussudio.Tests/RecordingQueue.Tests.cs`, `tests/Sussudio.Tests/MainWindowUiContract.Tests.cs:20002-20109` and dozens more
**Surfaced by:** Gate 2 CC-1 (shard 3) — flagged as P1 endemic
**What breaks:** Hundreds of tests do `AssertContains(sourceText, "FastStart = false")`. Refactoring the production code (renaming a local, reflowing an argument list) breaks tests without breaking behavior — and conversely, a subtle semantic bug that preserves the spelled-out source string passes. This is a methodology bug, not a code bug, but is large enough that the fix is "convert grep tests to behavior tests over a planned campaign."
**Suggested fix:** No single diff. Add a lint that fails CI on any new `AssertContains(sourceText, ...)` call; backfill behavior tests incrementally. Out of scope for the harden loop.

#### #23 — P1 — `MainViewModel.DeviceManagement.cs:152` — `SelectedDevice = Devices[0]` clobbers user choice on rescan (no `_isRecording` guard)
**Files:** `Sussudio/ViewModels/MainViewModel.DeviceManagement.cs:152`
**Surfaced by:** Gate 2 §45
**What breaks:** During a device rescan triggered by hot-plug, `SelectedDevice = Devices[0]` is unguarded. Mid-recording, a USB device flap clobbers the user's currently-recording device choice (the actual capture pipeline is unaffected because it's bound by handle, but the UI and the next-restart device id are now wrong).
**Suggested fix sketch:** Wrap in `if (!_isRecording)`; on `_isRecording == true`, keep the existing selection and log a `DEVICE_RESCAN_KEEP_SELECTION` breadcrumb. One-line patch but I have not read the surrounding context; saving for harden loop.

#### #24 — P1 — Network drop (UNC output path) mid-write surfaces as fatal cleanup that truncates the active file
**Files:** `Sussudio/Services/Recording/LibAvRecordingSink.cs` (error paths), `Sussudio/Services/Recording/RecordingArtifactManager.cs`
**Surfaced by:** Gate 3 "Network drop mid-write" (shard 1)
**What breaks:** When the user picks a UNC path and the network drops mid-recording, the IO exception escalates through `EnqueueRawVideoFrame` → fatal cleanup → moov-missing file. No retry, no fallback to a local spool.
**Suggested fix sketch:** Add a `_localSpoolPath` fallback in `RecordingArtifactManager` that the encoder writes to on UNC IO failure; upload to the UNC location post-stop. Multi-file; sketch only.

#### #25 — P1 — Disk full mid-export: `OrphanTempFileMinimumAge = 15min` leaves stale .tmp files for 15 minutes
**Files:** `Sussudio/Services/Flashback/FlashbackExporter.cs` (orphan cleanup)
**Surfaced by:** Gate 3 "Disk full mid-export 15min orphan" (shard 1) — top-3 severity
**What breaks:** On disk-full mid-export the temp file is left in place. The configured cleanup heuristic waits 15 minutes before deleting it. The user's perception is "the disk is still full." Worse, the temp file path uses the user's chosen output directory, so the corrupted partial is in their movie folder.
**Suggested fix sketch:** On `ENOSPC` / `HRESULT 0x80070070` (`ERROR_DISK_FULL`), delete the temp file immediately rather than waiting for the orphan-age threshold. One-hunk diff lives at the catch site in `FlashbackExporter.RunExport`.

#### #26 — P1 — `Mocked dependency drift` — `FakeProcessSupervisorImpl` matches on argument substrings; production argv changes silently fall through
**Files:** `tests/Sussudio.Tests/RecordingVerifier.Integration.Tests.cs` and friends
**Surfaced by:** Gate 3 "Mocked dependency drift" (shard 3)
**What breaks:** Tests inject a fake supervisor whose match is `args.Contains("--profile")`. Production argv refactor from `--profile` to `--verify-profile` passes the test (no match → fake returns default success) and ships green. Methodology bug; no single diff.

#### #27 — P1 — Log-key drift: classifier matches `operation=` but two call sites emit `op=range`
**Files:** `tools/Common/DiagnosticSessionRunner.cs` (logs at shard 805/809/825/829 use `operation=`; logs at shard 966 use `op=range`); classifier in `tests/Sussudio.Tests/PreviewPacingClassifier.Tests.cs`
**Surfaced by:** Gate 4 #10 shard 3
**What breaks:** Classifier silently misses the `op=range` events; the on-disk diagnostic timeline shows operations that the classifier never bucketed. Operators reviewing reports see a gap where ranges should be reported.
**Suggested fix:** Rename `op=` to `operation=` at the two anomalous call sites. One-line per site:

```diff
- Logger.Log($"FLASHBACK_RANGE_OP op=range start_pts={…}");
+ Logger.Log($"FLASHBACK_RANGE_OP operation=range start_pts={…}");
```
(Exact lines need to be located inside `DiagnosticSessionRunner.cs` shard 966 reference — harden loop will pin them.)

#### #28 — P1 — `ssctl --timeout` override silently undercuts long mutations
**Files:** `tools/ssctl/CommandHandlers.cs` (timeout plumbing)
**Surfaced by:** Gate 3 §E (shard 4)
**What breaks:** `ssctl … --timeout 5000 FlashbackExport` sets a 5s pipe response timeout on a command that legitimately takes 30s+. The transport-error envelope returns *and the server continues the export*, so the client retries, and now two concurrent exports collide. Worse, the operator sees `transport-error` and thinks the export failed.
**Suggested fix sketch:** Define per-command minimum timeouts in the catalog (FlashbackExport: 120s) and clamp `--timeout` from below.

#### #29 — P1 — `AutomationPipeClient` retry loop: 15 retries × 1s baseline can stack with per-attempt connect timeout (`connectTimeoutMs`) into long total wait
**Files:** `tools/Common/AutomationPipeClient.cs:132-178`, `tools/Common/AutomationPipeProtocol.cs:20-21`
**Surfaced by:** Gate 4 #11 (shard 4)
**What breaks:** Baseline cap is 15 retries × 1s = 15s, but `connectTimeoutMs` defaults are passed per-attempt; with `connectTimeoutMs=30000` (some callers), the total bound is 15 × (30s + 1s) ≈ 7.75 minutes for a single `not_ready` cascade. The Gate-4 "85min" claim was a worst-case figure factoring in per-call-site retry wrapping; in this hot path the actual ceiling is ~8min.
**Suggested fix:**
```diff
--- a/tools/Common/AutomationPipeClient.cs
+++ b/tools/Common/AutomationPipeClient.cs
@@ -130,9 +130,17 @@
     internal static async Task<AutomationPipeCommandResult> SendCommandWithResultAsync(
         string pipeName,
         string commandName,
         object? payload,
         int connectTimeoutMs,
         int responseTimeoutMs,
         string? authToken = null,
         bool includeResponseElement = false,
         CancellationToken cancellationToken = default)
     {
         var commandValue = AutomationPipeProtocol.ResolveCommand(commandName);
+
+        // Total time budget across retries — clients can override via the CT.
+        // Without this, 15 retries × (connectTimeoutMs + 1s) can stack into
+        // ~8 minutes for a single not_ready cascade, well past the user's
+        // patience.
+        var totalDeadline = Stopwatch.StartNew();
+        var totalBudgetMs = Math.Max(connectTimeoutMs * 3, 60_000);
 
         for (var attempt = 0; ; attempt++)
         {
             cancellationToken.ThrowIfCancellationRequested();
+            if (totalDeadline.ElapsedMilliseconds > totalBudgetMs)
+            {
+                throw new TimeoutException(
+                    $"Automation command '{commandName}' exceeded total retry budget of {totalBudgetMs}ms.");
+            }
```

#### #30 — P1 — Pre-built tool assemblies on disk: tests load `tools/{McpServer,ssctl}/bin/Debug/net8.0/*.dll` directly with no project reference
**Files:** `tests/Sussudio.Tests/Sussudio.Tests.csproj`, multiple test files
**Surfaced by:** Gate 2 CC-3 (shard 3)
**What breaks:** Tests load a stale binary; a change in `tools/McpServer/*.cs` that breaks contract is invisible to tests until the test author rebuilds the tool. The CI matrix happens to rebuild because of build ordering, but local dev does not. False-positive green tests on stale code.
**Suggested fix sketch:** Add `ProjectReference` from the tests csproj to the tool csprojs. Single-line addition per ref; out of scope for harden as it can break the existing test fixture loaders that rely on Assembly.LoadFile of a known path.

#### #31 — P1 — Multi-hour pipe reconnect: ssctl/MCP session caches env at start; server restart leaves them pointing at a dead pipe
**Files:** `tools/Common/AutomationPipeClient.cs`, `tools/McpServer/Program.cs`
**Surfaced by:** Gate 3 §D (shard 4)
**What breaks:** A long-running MCP host caches `SUSSUDIO_AUTOMATION_PIPE` at start; when the Sussudio app restarts (different pipe id), the host returns connect timeouts indefinitely. Re-reading the env on each connect would fix it.
**Suggested fix sketch:** Read pipe name from env on every connect attempt rather than once at MCP start.

#### #32 — P1 — Test scenario list duplicated 5 ways (NormalizeScenario in 5 sites)
**Files:** `tools/Common/DiagnosticSessionRunner.cs`, `tools/ssctl/CommandHandlers.cs:2377`, `tools/McpServer/Tools/DiagnosticSessionTools.cs:9862`, and two test sites
**Surfaced by:** Gate 2 §Diagnostic-session scenario-string drift, Gate 4 #9, Gate 5 #24 (shard 3)
**What breaks:** Adding a new scenario name to one site silently leaves it as "unknown" in the other four. Maintainability/correctness P1 because it tends to fail open (unknown scenario silently does nothing).
**Suggested fix sketch:** Single shared `ScenarioCatalog` static class in `tools/Common/` referenced by all five sites; remove the duplicated literal arrays.

#### #33 — P1 — `Flashback buffer not active` literal duplicated 4 times across product+tests
**Files:** `Sussudio/Services/Capture/CaptureService.cs`, `tools/ssctl/CommandHandlers.cs`, two test files
**Surfaced by:** Gate 4 violation #3 shard 3
**What breaks:** Same drift hazard as #32. If the user-facing message is reworded in one place, the source-text grep tests fail. If only the test is reworded, drift goes unnoticed.
**Suggested fix sketch:** Define as `internal const string CaptureService.FlashbackBufferNotActiveMessage`; reference from all sites.

#### #34 — P1 — MCP fake (test fixture) hard-codes one envelope shape
**Files:** `tests/Sussudio.Tests/McpToolSurface.Tests.cs`
**Surfaced by:** Gate 4 violation #8 shard 3
**What breaks:** Fake returns a fixed envelope; production responses now include alternate shapes (e.g., `not_ready` retry hint) that the fake never emits. Tests pass; production fails.
**Suggested fix sketch:** Parameterize the fake to return different envelope shapes per call. Test-fixture change only.

### Audio

#### #35 — P2 — `_audioEnabled` / `_microphoneEnabled` / `_started` cross-thread reads in LibAv sink (non-volatile)
**Files:** `Sussudio/Services/Recording/LibAvRecordingSink.cs:54-56, 232, 266, 458, 485, 313`
**Surfaced by:** Gate 5 #3 (shard 2)
**What breaks:** On ARM64 (Windows on ARM), the stale-read window is observable. P3 on x64.
**Suggested fix:** Switch the three fields to `volatile bool` declarations.

#### #36 — P2 — `cuDeviceGet(out _, 0)` hard-coded ordinal — wrong device on Optimus / multi-GPU
**Files:** `Sussudio/Services/Gpu/CudaD3D11Interop.cs:74`
**Surfaced by:** Gate 5 #8 (shard 2)
**What breaks:** On dual-GPU laptops (NVIDIA + integrated), CUDA picks ordinal 0 which may not be the same adapter as the D3D11 device. Silent garbage frames.
**Suggested fix sketch:** Use `cuD3D11GetDevice` with the D3D11 adapter LUID to select the matching CUDA ordinal.

#### #37 — P2 — `WasapiAudioCapture.expectedIntervalMs` unit confusion — ratio compared as milliseconds
**Files:** `Sussudio/Services/Audio/WasapiAudioCapture.cs:705-708`
**Surfaced by:** Gate 5 #27 (shard 2)
**What breaks:** `OutputSampleRate / _captureFormat.SampleRate` is a dimensionless ratio (e.g., 48000/44100 ≈ 1.088), compared as if it were milliseconds against `WaitTimeoutMs = 100`. The `Math.Max(100, ratio*4)` always picks 100. Severe-gap audio detection is effectively disabled.
**Suggested fix:** Compute the expected interval in ms from frame count and sample rate: `expectedIntervalMs = (frameCount * 1000.0) / _captureFormat.SampleRate`.

### Tooling & build scripts

#### #38 — P2 — `stage-builds.ps1 /MIR` robocopy mirror with `Configurations` parameter — destructive if attacker plants matching tree
**Files:** `tools/stage-builds.ps1`
**Surfaced by:** Gate 2 §stage-builds (shard 4 — downgraded P0→P1 originally, settled P2 here because both `$source` and `$dest` use same `$configuration` and a `Test-Path` check guards the source)
**What breaks:** `robocopy /MIR` deletes any file in `$dest` not present in `$source`. No `ValidateSet` on `-Configurations`, so a typo or attacker-controlled value can shadow into a sensitive path.
**Suggested fix:** Add `[ValidateSet("Debug","Release")]` to the `-Configurations` parameter.

#### #39 — P2 — `validate_hdr.ps1` accepts HDR10 with only one of mastering/content-light metadata
**Files:** `tools/validate_hdr.ps1`
**Surfaced by:** Gate 2 §validate_hdr (shard 4)
**What breaks:** HDR10 requires both `mastering_display_colour_volume` and `content_light_level`; the check is `-not ($hasMastering -or $hasContentLight)` which passes if either is present. Validator emits "HDR PASS" on a non-compliant file.
**Suggested fix:** Change to `-not ($hasMastering -and $hasContentLight)`.

#### #40 — P2 — `validate_hdr.ps1 Analyze-Cadence` excludes intervals >5s — drops exactly the stutters it should catch
**Files:** `tools/validate_hdr.ps1`
**Surfaced by:** Gate 2 §validate_hdr (shard 4)
**What breaks:** A 5-second pause in delivery is the worst-case failure; excluding it makes the analysis blind to the case it most needs to detect.
**Suggested fix:** Remove the upper-bound filter.

#### #41 — P2 — `RecordingVerifier` builds ffprobe args via string concat with `"…"` quotes
**Files:** `Sussudio/Services/Recording/RecordingVerifier.cs:87`
**Surfaced by:** Gate 5 #1 (shard 2)
**What breaks:** Paths with embedded double-quote characters break the command line. Rare but real on Windows.
**Suggested fix:** Use `ProcessStartInfo.ArgumentList.Add(outputPath)`.

#### #42 — P2 — `pipe accept loop` serializes connections; one slow client blocks all others
**Files:** `Sussudio/Services/Automation/NamedPipeAutomationServer.cs:457` + accept loop
**Surfaced by:** Gate 5 #5 (shard 2)
**What breaks:** Multiple parallel ssctl/MCP/StreamDeck connections queue serially. Documented `MaxAllowedServerInstances` is the cap, but the accept loop doesn't fan-out connection handling concurrently.
**Suggested fix sketch:** Spawn each accepted connection's handler on the thread pool; cap concurrent active handlers separately.

### Lower-severity findings (P3/P4)

| ID | Tier | Category | File:line | One-line | Surfaced by |
|----|------|----------|-----------|----------|-------------|
| 43 | P2 | Recording | `Sussudio/Services/Capture/CaptureService.cs` SegmentCount | `File.Exists` called O(N) per UI tick — perf hotspot at long sessions | Gate 5 #18 (shard 1) |
| 44 | P2 | Recording | `Sussudio/Services/Audio/WasapiAudioCapture.cs` priority swap | `BelowNormal` thread priority leaks onto pool thread on restore-throw | Gate 5 #27 (shard 1) |
| 45 | P2 | Recording | `Sussudio/ViewModels/MainViewModel.cs` stale-session cleanup | Synchronous filesystem walk under session lock; UI freeze on slow disks | Gate 2 §57, Gate 5 #20 (shard 1) |
| 46 | P2 | Recording | `Sussudio/Services/Capture/CaptureService.cs` IsFlashbackRecordingBackendActive | Unlocked reads of three fields produce inconsistent `GetRecordingStats` | Gate 4 #4 (shard 1) |
| 47 | P2 | Recording | `Sussudio/Services/Recording/LibAvRecordingSink.cs` pending settings clear | Settings cleared on finalize fail; log says "deferred" — log-vs-behavior drift | Gate 4 #5 (shard 1) |
| 48 | P2 | Automation | `Sussudio/Models/SnapshotPropertyCache` | Reflection cache drifts under field-rename refactors | Gate 4 #12 (shard 1) |
| 49 | P2 | Preview | `Sussudio/Services/Capture/MfSourceReaderVideoCapture.cs` EnqueueRawVideoFrame | Span overload doesn't validate `expectedSize == width*height*bpp` (lease overload does) | Gate 4 #17 (shard 2) |
| 50 | P2 | Automation | `Sussudio/Services/Capture/CaptureSessionCoordinator.cs` 9-call MCP batch | Non-atomic; partial apply on failure | Gate 2 §configure-capture (shard 4) |
| 51 | P2 | Automation | `tools/ssctl/PipeTransport.cs` 60000 vs MCP 15000 floor | Wait-timeout drift between tools | Gate 4 #1 (shard 4) |
| 52 | P2 | Automation | `tools/Common/AutomationPipeSecurityPolicy.cs` ShouldDisableDefaultSecurityFallback | Inverted-reading name; a refactor that flips the predicate is silent | Gate 4 #15 (shard 4) |
| 53 | P2 | Tooling | `tools/capture-rewrite-live-baseline.ps1` Wait-AutomationAvailable | Catch-all swallows auth-token mismatch — polls 60s with no diagnostic | Gate 2 §Baseline (shard 4) |
| 54 | P2 | Tests | `tests/Sussudio.Tests/CaptureSessionCoordinator.Tests.cs` UninitializedObject pattern | `RuntimeHelpers.GetUninitializedObject` bypasses ctor validation in tests | Gate 2 CC-2 (shard 3) |
| 55 | P2 | Tests | `tests/Sussudio.Tests/CaptureService.Snapshots.Tests.cs` source-text grep with multi-line literal | Pins format not behavior | Gate 4 #4 (shard 3) |
| 56 | P2 | Tests | `tests/Sussudio.Tests/Flashback.Tests.cs:200` Thread.Sleep literal | Test prevents the correct fix | Gate 3 Additional #4 (shard 3) |
| 57 | P2 | GPU | `Sussudio/Services/Flashback/FlashbackPlaybackController.cs` PreviewDetach indefinite defer | Genuine freeze surface (requires app restart) | Gate 2 §444 (shard 1) |
| 58 | P2 | Recording | `Sussudio/Services/Recording/RecordingVerifier.cs` verification gate no timeout | Hung verifier blocks all subsequent verifies | Gate 2 §781 (shard 1) |
| 59 | P2 | Tooling | `tools/stage-builds.ps1` robocopy 4-7 treated as success | Code 4 (mismatch) silently passes | Gate 2 §stage-builds (shard 4) |
| 60 | P2 | Tooling | `tools/reliability-gates.ps1` test-runner concurrent-build race | Two parallel `stage-builds.ps1` corrupt outputs | Gate 2 §reliability-gates (shard 4) |
| 61 | P2 | Probes | `tools/NativeXuAudioProbe/*` native-xu probes leak on exception | Leaves RTK_IO DLL state inconsistent | Gate 2 §native-xu (shard 4) |
| 62 | P3 | Recording | `Sussudio/Services/Capture/CaptureService.cs` BeginRecording rollback | Not transactional | Gate 2 §259 (shard 1) |
| 63 | P3 | Recording | `Sussudio/Services/Recording/LibAvRecordingSink.cs:1409-1410` substring "cancel" classifier | Misclassifies legit cancel messages | Gate 5 #9 (shard 1) |
| 64 | P3 | Flashback | `Sussudio/Services/Flashback/FlashbackExporter.cs` ClassifyFlashbackExportFailureKind | Substring cascade | Gate 5 #10 (shard 1) |
| 65 | P3 | Flashback | `Sussudio/Services/Flashback/FlashbackBufferManager.cs` PurgeCompletedSegments | Byte-accounting drift on completed segments | Gate 5 #19 (shard 1) |
| 66 | P3 | Flashback | `Sussudio/Services/Flashback/FlashbackBufferManager.cs` FindDuplicateSegmentPathIndex | O(n²) — hot path at saturation | Gate 5 #25 (shard 1) |
| 67 | P3 | Recording | `Sussudio/Services/Recording/LibAvRecordingSink.cs:16100` SkipVideoFrame race | Concurrent caller race | Gate 5 #15 (shard 1) |
| 68 | P3 | Audio | `Sussudio/Services/Audio/WasapiAudioCapture.cs` audio enqueue eviction-then-retry | Drops without distinct counter for "retry-after-evict-also-failed" | Gate 5 #15 (shard 2) |
| 69 | P3 | Preview | `Sussudio/Services/Gpu/ParallelMjpegDecodePipeline.cs` SetSkipCpuReadback | Silent perf degradation on software-MJPEG fallback | Gate 2 §149 (shard 1) |
| 70 | P3 | Preview | `Sussudio/MainWindow.PreviewStartup.cs` first-frame timeout silent | `FrameCaptureTimeoutMs=5000` — no UI feedback on hit | Gate 2 §55 (shard 1) |
| 71 | P3 | UX | `Sussudio/ViewModels/MainViewModel.Capture.cs:47` auto-start preview swallow | Non-cancellation surfaced only via StatusText | Gate 2 §47 (shard 1) |
| 72 | P3 | UX | `Sussudio/MainWindow.WindowManagement.cs` double 120s stop budget | Total close window 240s — user has no signal they're in second wait | Gate 2 close-window (shard 2) |
| 73 | P3 | UX | `Sussudio/ViewModels/MainViewModel.Capture.cs` IsRecording before EncodingFailed check | UI lies briefly | Gate 2 start-recording (shard 2) |
| 74 | P3 | Stats | `Sussudio/ViewModels/StatsPresentationBuilder.cs` NaN→0.0 unspecified | Math.Max semantics would silently differ for negative input | Gate 2 stats overlay (shard 3) |
| 75 | P3 | Automation | `Sussudio/Services/Automation/AutomationCommandDispatcher.cs` Ordinal token compare | Timing-oracle (local-only attack surface, low) | Gate 4 #3 (shard 2) |
| 76 | P3 | Automation | `Sussudio/Services/Automation/NamedPipeAutomationServer.cs:270` ReadLineAsync unbounded | Pre-auth OOM (pipe ACL limits exposure) | Gate 4 #6 (shard 2) |
| 77 | P3 | Automation | `Sussudio/Services/Capture/CaptureSessionCoordinator.cs` unbounded coordinator channel | No per-client rate limit; UI starvation worst case | Gate 4 #7 (shard 2) |
| 78 | P3 | Automation | `Sussudio/ViewModels/MainViewModel.Automation.cs` IsAutomationReady | Returns true when pipeline uninitialized but `Devices.Count > 0` | Gate 4 #11 (shard 2) |
| 79 | P3 | Automation | `Sussudio/Services/Capture/CaptureSessionCoordinator.cs` _suppressMicrophoneMonitorUpdate | Non-volatile cross-thread | Gate 4 #12 (shard 2) |
| 80 | P3 | Automation | `Sussudio/Services/Capture/CaptureSessionCoordinator.cs` eviction-pause counter | Can leak on dispatcher-enqueue failure | Gate 4 #13 (shard 2) |
| 81 | P3 | Window | `Sussudio/MainWindow.WindowManagement.cs:20809` Closed handler ordering | Tears down frame producer before encoder finalize wait — file short by hundreds of ms | Gate 4 #14 (shard 2) |
| 82 | P3 | Flashback | `Sussudio/Services/Flashback/FlashbackPlaybackController.cs` ExecuteFlashbackAction.Play | Collapses seek-fail vs play-fail into one bool | Gate 4 #15 (shard 2) |
| 83 | P3 | Tests | `tests/Sussudio.Tests/PipeTransport.Tests.cs` RetryAfterMs edge cases | `-1`, `int.MaxValue`, string-form not tested | Gate 2 PipeTransport (shard 3) |
| 84 | P3 | Tests | `tests/Sussudio.Tests/DiagnosticSessionRunner.cs` mutable result | Arrays publicly set-able post-init | Gate 2 (shard 3) |
| 85 | P3 | Tests | `tests/Sussudio.Tests/DiagnosticSessionRunner.cs` SampleLoopAsync orphan snapshot | Snapshot added before checkpoint await → throw orphans it | Gate 2 (shard 3) |
| 86 | P3 | Native | `tools/NativeXuAudioProbe/Program.cs` BuildAtFrameWithPayload | Payload length ≥ 252 silently corrupts AT frames | Gate 5 #1 (shard 3) |
| 87 | P3 | Native | `tools/NativeXuAudioProbe/Program.cs` SendI2cViaAtAsync envelope strip | Off-by-two in cmd-field accounting | Gate 5 #2 (shard 3) |
| 88 | P3 | Native | `tools/NativeXuAudioProbe/Program.cs` respLen sentinel collapse | `respLen ≤ 0` and `> 1024` both collapse to `Array.Empty<byte>()` | Gate 5 #3 (shard 3) |
| 89 | P3 | Tooling | `tools/Common/AutomationSnapshotFormatter.cs` cleanup CTS not linked | `CreateCleanupCts(timeout)` doesn't link the outer cancellationToken | Gate 5 #9 (shard 3) |
| 90 | P3 | Tooling | `tools/Common/AutomationSnapshotFormatter.cs` ObserveTaskAfterFaultAsync orphans | 2s sleep × 14 orphans = 28s post-cancel | Gate 5 #10 (shard 3) |
| 91 | P3 | Tooling | `tools/Common/AutomationSnapshotFormatter.cs` snapshot accumulation | 50MB / 30× iteration for max/p99 | Gate 5 #11 (shard 3) |
| 92 | P3 | Tooling | `tools/ssctl/Formatters.cs` locale-dependent parsing | One TryParse path is non-invariant culture (de-DE etc.) | Gate 5 #15 (shard 3) |
| 93 | P3 | Tooling | `tools/Common/AutomationSnapshotFormatter.cs` FormatD3DP99Bottleneck NaN drop | NaN-filter strips dominant stage when one slot is NaN | Gate 5 #16 (shard 3) |
| 94 | P3 | Tooling | `tools/Common/AutomationSnapshotFormatter.cs` NonNegativeDelta hides counter resets | Stale-load snapshots produce nonsense ages | Gate 5 #19 (shard 3) |
| 95 | P3 | Tooling | `tools/Common/AutomationSnapshotFormatter.cs` initialSnapshot empty-sentinel | `initialSnapshotKnown` exists but not consulted on every state-mutating decision | Gate 5 #29 (shard 3) |
| 96 | P3 | Tooling | `tools/AutomationClient/Program.cs` exit 3 vs pipe-protocol error | Semantic confusion | Gate 2 AutomationClient (shard 4) |

(P3/P4 entries continue past row 96 in the original gate-6 outputs but cluster into duplicates of the rows above. The harden loop will materialize the remaining ~70 P4 rows when it touches the surrounding code; the master list lives in `.duat/gate-6-shard-*.md`.)

## Unresolved (needs investigation)

- **Gate 2 §53 — `CleanupStaleRootSegmentFiles` with no marker check** — `Sussudio/Services/Flashback/FlashbackBufferManager.cs` — surfaced by Gate 2. What would resolve it: grep for any producer that writes `fb_*` files into temp root (none in shard 1 excerpt). If none exists, drop as latent.
- **Gate 2 §recursive reinit via ApplySourceTelemetrySnapshot** — `Sussudio/ViewModels/MainViewModel.Capture.cs` — surfaced by Gate 2 shard 2. What would resolve it: trace `ApplySourceTelemetrySnapshot(allowAutoRetarget:true)` to verify `_previewReinitializeGate` blocks the recursion.
- **Gate 2 §events fire during MainViewModel ctor** — `Sussudio/ViewModels/MainViewModel.cs` — surfaced by Gate 2 shard 2. What would resolve it: read CaptureService ctor to confirm `GetLatestSourceTelemetrySnapshot()` cannot raise events synchronously.
- **Gate 3 §"Idle 8+ hours" — eviction pause counter asymmetric leak** — `Sussudio/Services/Capture/CaptureSessionCoordinator.cs` — surfaced by Gate 3 shard 1. Hypothetical; needs a long-running soak test to observe.
- **Gate 3 §"OOM allocation failure" — `Marshal.AllocHGlobal` zero-return handling** — surfaced by Gate 3 shard 1. What would resolve it: enumerate all AllocHGlobal call sites and verify each checks the return for `IntPtr.Zero`.
- **Gate 4 #7 — `_bufferManager.LatestPts` read pre-detach race** — `Sussudio/Services/Flashback/FlashbackBufferManager.cs` (line ~12345-12459 in pack, search by `_bufferManager.LatestPts`) — surfaced by Gate 4 shard 1. Imprecise line numbers in the original finding; harden loop should re-pin.
- **Gate 5 #22 — `_activeSegmentStartPtsTicks` torn pair** — surfaced by Gate 5 shard 1. What would resolve it: read `GetDefaultActiveSegmentStartPts` body to confirm whether the pair is atomically loaded.
- **Gate 5 #43 — USB-3.0 MJPG override `nativeFormatName` as routing key** — surfaced by Gate 5 shard 1. P2 if downstream consumes the field as a routing key, P4 if diagnostic-only. Resolves by `nativeFormatName` consumer search.
- **Gate 2 §Verify timing oracle** — `Sussudio/Services/Automation/AutomationCommandDispatcher.cs:~1101` IsAuthorized — surfaced by Gate 2 shard 4. What would resolve it: confirm the comparator is `string.Equals(..., StringComparison.Ordinal)` and not a constant-time path; for local pipe the threat model is thin so likely P3.
- **Gate 2 §preview-frame 10-bit luminance** — `tools/McpServer/Tools/PreviewFrameCaptureTools.cs` — surfaced by Gate 2 shard 4. Cross-shard server-side scaling. Resolves via shard 2 reconciliation.
- **Gate 4 #3 shard 4 — MCP `flashback_action` missing `nudge`** — `tools/McpServer/Tools/FlashbackTools.cs` — surfaced by Gate 4 shard 4. Cannot verify without `AutomationFlashbackAction` server enum from shard 2.
- **Gate 4 #5 shard 3 — AssertDeclaredConfigProperties / DiagnosticSessionResult collision** — surfaced by Gate 4 shard 3. Hypothetical until a concrete `AssertDeclaredConfigProperties` test exists for `DiagnosticSessionResult`.
- **Gate 2 Flashback init stale-session collision regex** — `Sussudio/Services/Flashback/FlashbackBufferManager.cs` Initialize cleanup heuristic — surfaced by Gate 2 shard 3. What would resolve it: read the live `IsPlausibleFlashbackSessionDirectoryName` pattern to confirm whether `fb_init_pts_*` matches.

## Gaps Identified (Gate 6)

Aggregated across all 4 Gate-6 shard outputs (Part B):

**Bug categories not covered by any gate:**
- WinUI 3 dispatcher-thread-affinity contract violations for `[ObservableProperty]` setters mutated from background handlers (`OnCaptureError`, `OnFatalError`, `OnFlashbackBufferUpdated`).
- `Environment.FailFast` reentry — `Logger.LogFatalBreadcrumb` writes to disk inside the unhandled-exception path. If that write throws on a parallel `Logger.Shutdown` race, the throw is unobserved.
- `DispatcherQueue.TryEnqueue` returning `false` after shutdown — dozens of call sites ignore the return value, silently dropping state mutations during window close.
- MMCSS thread-registration leaks — symmetry of register/unregister was not enumerated.
- JsonSerializer source-gen vs reflection fallback — settings added to `UserSettings` but not to `SettingsJsonContext` produce a silent perf cliff on every save.
- NVENC encoder reconfiguration rate limits — `CycleFlashbackEncoderSettings` rapid-fire is undocumented.
- `CompositionTarget.Rendering` event-handler lifecycle — failed unsubscription keeps the window alive past `Closed`.
- ArmClose race with natural close — overlapping flows not analyzed.
- Localization / RTL / non-ASCII in user strings beyond output paths (artifact display names, recovery marker session ids, automation `CorrelationId`).
- Thread-pool starvation / sync-over-async deadlocks beyond the named `AutomationDiagnosticsHub.Dispose` example (`RecordingArtifactManager.CreateContextAsync`, `StorageFolder.GetFolderFromPathAsync`, `WasapiAudioCapture.InitializeAsync`).
- Disposal ordering of `D3D11PreviewRenderer` vs `UnifiedVideoCapture` vs `FlashbackDecoder` sharing the same D3D11 device.
- Auth-token lifecycle / timing-attack safety / rotation race.
- PII in INFO-level log lines (output paths, device names containing usernames).
- WinAppSDK packaged vs unpackaged identity divergence (`MfInteropHelpers.AddStartupReference`, `Process.GetCurrentProcess`).
- Pipe-protocol fuzzing / malformed-response robustness (missing trailing newline, garbled UTF-8 in `Message`).
- TOCTOU on `Path.GetFullPath` in `ValidatePath`.
- `AutomationCommandKind` source-link synchronization between csprojs that link the file (`ssctl.csproj`, `McpServer.csproj`).
- Locale/culture in PowerShell JSON `[double]::Parse` paths.
- Long-path `\\?\` prefix support.
- ETW session leak on probe crash.
- Pipe ACL/SID side — what SID gets the pipe ACL when the explicit descriptor path fails.
- MCP JSON-RPC framing on stdio for invalid host input.

**Files with zero or near-zero findings (potential blind spots):**
- `Sussudio/Services/Preview/D3D11PreviewRenderer.Rendering.cs` (one finding, but rendering loop / shader compile / HDR tone-mapping unprobed).
- `Sussudio/Models/AutomationContracts.cs` (one finding; rich enum surfaces for wire stability unprobed).
- `Sussudio/Services/Telemetry/NativeXuAtCommandProvider.cs` (two findings; KsExtensionUnit IOCTL marshalling unprobed).
- `Sussudio/Services/Capture/MfSourceReaderVideoCapture.cs` (three findings; MF event callback re-entrancy, COM-apartment, IntPtr lifetime unprobed).
- `Sussudio/MainWindow.xaml.cs` (ctor service wiring, env reads, DWM setup unprobed).
- `Sussudio/Services/Capture/PooledVideoFrame.cs` (lease/owner protocol misuse from callers).
- `Sussudio/Services/Capture/FrameFingerprintCadenceTracker.cs` (approximation propagation to classifiers).
- `Sussudio/Services/Gpu/NvdecMjpegDecoder.cs` (two Initialize overloads with different ownership semantics — caller mismatch unprobed).
- `Sussudio/Services/Runtime/FfmpegRuntimeLocator.cs` (cached-path-becomes-invalid).
- `Sussudio/Services/Configuration/SettingsService.cs` (load-side forward-compat).
- `Sussudio/MainWindow.StatsOverlay.cs` (timer-lifecycle vs window-close ordering).
- `tools/Common/JsonOptions.cs`, `tools/NativeXuAudioProbe/{ToolCaptureDevice,ToolLogger}.cs` (no findings; drift hazard from duplicated types).
- `tools/RtkIoShim/rtk_io_shim.def` (export-mismatch maintenance risk).
- `tools/McpServer/Program.cs` (no security review of `WithToolsFromAssembly` attribute discovery).
- `tools/RtkIoShim/build.bat` (no version stamping or checksum).
- `tools/CoreAudioEndpointProbe/Program.cs` (volume-mutation rollback under exception unprobed).
- `tests/Sussudio.Tests/App.xaml.Tests.cs` ~40 lines — cold-start path unexercised.
- `tests/Sussudio.Tests/DisplayFormatters.Tests.cs` — missing cultures (de-DE, ja-JP) and edge values (NaN, Infinity, MaxValue).
- `tests/Sussudio.Tests/Flashback.Decoder.Tests.cs` — decoder lifecycle / EOF / seek-past-end unexercised.

**Domain-specific gaps (WinUI 3 / WASAPI / D3D11 / MF / NVENC):**
- D3D11 device-removed (TDR) recovery loop and `SharedD3DDeviceManager._multithread.Enter()` on a removed device.
- WASAPI exclusive-mode failover.
- MF source-reader thread starvation under synchronous fan-out (preview + flashback + recording exceeding frame interval).
- NVENC capabilities probing race — first probe failure caches `EncoderSupport.Empty` for process lifetime.
- AVCC vs Annex-B NAL framing for HEVC in MP4 vs MKV.
- HDR static metadata propagation `mastering_display_colour_volume` SEI message generation.
- Audio resampler integer-remainder carry across pause/resume.
- WASAPI device removal (USB unplug) HRESULT handling.
- D3D11 deferred vs immediate context contention.
- SwapChainPanel SizeChanged during fullscreen transition.
- `MFShutdown` reference-count underflow / overflow.
- CUDA primary-context refcount underflow on partial-init catch + Dispose.
- NVML driver-update mid-session.
- MMCSS task registration leaks for `FlashbackPlaybackController` thread.
- DXGI swapchain RDP/Remote Desktop transition.
- HDR metadata range/clip in ffmpeg muxer.
- WinAppSDK `DispatcherQueue` shutdown ordering on window close.
- MediaFoundation reader buffer pool exhaustion (`MF_E_NOTACCEPTING`).
- High-DPI / per-monitor DPI awareness changes during capture.
- GPU adapter switch (hybrid graphics laptops).
- ETW session leak on probe crash.
- RtkIoShim load order vs Detours / other IAT-patching tools.
- NVENC fallback to libx264 silently producing different file shape.
- MF source disconnect (HDMI unplug) → counter freeze → confusing test failure.

## Methodology

- **Gate 1 (Contracts):** ~440 contract entries extracted across 4 shards, with ambiguity tags flagging future findings.
- **Gate 2 (Journeys):** 14 critical + 9 destructive journeys traced; hundreds of hazard rows clustered into ~90 unique root causes (heavy clustering on rapid-settings, emergency-stop budget, eviction-pause leak).
- **Gate 3 (Failure modes):** Adversarial user + hostile environment + resource pressure scenarios; produced ~40 distinct findings across 4 shards.
- **Gate 4 (Violations):** Contract-vs-usage cross-reference; 17+17+22 violations across shards.
- **Gate 5 (Standard review):** Traditional code quality; 50+35+30+60 raw findings.
- **Gate 6 (Self-critique):** Verdict & dedup applied per shard (89 confirmed shard 1, 56 confirmed shard 2, 62 confirmed shard 3, 132 confirmed shard 4); 6+2+2+3 unresolved noted; 3 false positives dropped.
- **Gate 7 (Synthesis):** This report. Clustered ~339 confirmed → 96 ranked findings by root cause; 8 P0 + 21 P1 written with diffs or diff-sketches; remaining P2-P4 tabulated.

## Cross-shard reconciliation notes (Gate 7 originated)

- Shard 4 §preview-frame 10-bit luminance NEEDS-INVESTIGATION → resolves against shard 2 server-side `PreviewColorProbeTool.cs`. The shard 2 implementation rescales to 8-bit before luminance compute, so the shard-4 `AverageLuminance 8-bit assumption` is correct and the shard-4 finding stands as P4.
- Shard 1 Gate 5 #43 USB-3.0 MJPG `nativeFormatName` consumer search → fan-out check in shard 1 capture configuration shows the field is diagnostic-only, so the finding settles at P4 (not P2).
- Shard 4 cross-cutting #7 (AutomationCommandKind ID drift) cross-references shard 3 Gate 2 CC-3 (stale tool binaries on disk) and shard 4 §reliability-gates concurrent-build race — these three are the same root cause expressed in three operational surfaces. P0 finding #5 above is the consolidated entry.
- Shard 2 Gate 4 #2 (`ReinitializeDeviceAsync` no IsRecording guard) is mechanically the same bug Gate 3 "Rapid sequential setting changes" describes from the user perspective — P0 finding #1 above is the consolidated entry.

---

## Hardening Run — 2026-05-11

**Branch:** `duat/harden-2026-05-10-1055`
**Reviewer 5:** Claude Haiku fallback (codex:rescue probe skipped to conserve budget)
**Scope:** All 8 P0 findings attempted; P1+ deferred to budget cap (50% session time).

| # | Severity | Title | Status | Tally | Commit |
|---|----------|-------|--------|-------|--------|
| 1 | P0 | `ReinitializeDeviceAsync` IsRecording guard | FIXED | 3/5 APPROVE | 462532a |
| 2 | P0 | Single-instance Mutex in App.OnLaunched | FIXED | 5/5 APPROVE | bef7ce2 |
| 3 | P0 | FlashbackExport `force` flag (refuse-on-collision) | FIXED | 5/5 APPROVE | ba59a7a |
| 4 | P0 | `SetFlashbackEnabled` restore guard (compare current vs initial) | FIXED | 3/5 APPROVE | ed1b4a7 |
| 5 | P0 | AutomationCommandKind manifest revision handshake | FIXED | 5/5 APPROVE | d50f342 |
| 6 | P0 | Ctrl-C / pipeline-stopped cleanup in tools | FIXED | 4/5 APPROVE | 55cd298 |
| 7 | P0 | DiagnosticSessionRunner per-output-dir FileStream lock | FIXED | 4/5 APPROVE | b6707fe |
| 8 | P0 | `IsRecoverableUnhandled` allowlist (DXGI/MF/AUDCLNT HRESULTs + IOException + TimeoutException + AggregateException unwrap) | FIXED | 5/5 APPROVE | ed265ca |
| 9 | P1 | `CaptureService.UpdateEncodingSettings` transition lock | FIXED | 5/5 APPROVE | 497b2d3 |
| 10 | P1 | Mic toggle skips device wiring during recording | FIXED | 5/5 APPROVE | 24634ea |
| 11 | P1 | `LibAvRecordingSink` stale moov on drain timeout | FIXED | 4/5 APPROVE | f02d8ae |
| 12 | P1 | Emergency-stop budget cascade (split-constant, R2) | FIXED | 4/5 APPROVE R2 | 01a14c4 |
| 13 | P1 | `LibAvEncoder.InitializeFFmpeg` strict in App ctor | FIXED | 5/5 APPROVE | ca530d1 |
| 14 | P1 | `AutomationDiagnosticsHub.Dispose` deadlock risk | FIXED | 4/5 APPROVE | 4073ff0 |
| 15 | P1 | GPU device-removed purges flashback (R2) | FIXED | 4/5 APPROVE R2 | 8e1bcde |
| 16 | P1 | Sleep/hibernate wake → black preview + silent audio | FIXED | 5/5 APPROVE | e453b4e |
| 19 | P1 | ssctl vs MCP synthetic-error envelope shape (R2 + hotfix) | FIXED | 4/5 APPROVE R2 | 2140389 + 5bef76a |
| 20 | P1 | Snapshot field-name drift server vs client formatters | WITHDRAWN | premise false (`CurrentRecordingPath` does not exist) | — |
| 17, 18, 21–34 | P1 | 16 remaining P1 entries (see `.duat/fix-log.md`) | BLOCKED | user paused harden loop | — |
| P2–P4 | various | 67 lower-severity entries | BLOCKED | user paused harden loop | — |

**Aggregate:** 17 FIXED (8 P0 + 9 P1) · 1 WITHDRAWN · 83 BLOCKED (user paused) · 0 false-positive · 13 unresolved (carried from Gate 6, not retried this run).

**Hardening Run notes (P0 pass):**
- The two 3/5 commits (#1, #4) each had two REQUEST CHANGES votes on TOCTOU / sibling-pattern grounds. Concerns are logged in `.duat/fix-log.md` for follow-up rather than retried (Iron Law: surgical fix only; sibling concerns belong in their own finding).
- A NEW P0/P1 hazard was surfaced during Finding #4's panel: `tools/capture-rewrite-live-baseline.ps1:1001-1007` `SetPreviewEnabled` restore has the same purge hazard as #4, with `CaptureService.cs:3434 DisposePreviewPipelineAsync(..., purgeFlashbackSegments: true)` as the data-loss path. **Filed for the next `/duat` run.**

**Continue-run notes (2026-05-11):**
- Resumed via `/duat --continue --harden`. Branch resolved from `manifest["harden_branch"]` (Finding #4 of duat-skill — collision-safe resume).
- 9 P1 findings committed under the 5-reviewer panel rule (3/5 threshold). #15 and #19 needed a Round 2 after R1 caught regressions in R1; #12 took R2 after a 0/5 R1 (`StopTimeoutMs` change affected the normal stop path). #19 also needed a single-line hotfix (`AssertEqual(true, ...)` substituted for nonexistent `Assert(bool, string)` helper).
- #20 (snapshot field-name drift) was WITHDRAWN — the premise referenced a `CurrentRecordingPath` field that does not exist on either server or client snapshot DTOs. No drift to fix.
- **Deferred (researcher diffs ready, panels not run):** #21 (AuthToken env leak — clear after read in `MainWindow.xaml.cs:321`), #23 (`SelectedDevice` clobber on mid-recording rescan — `IsRecording` guard around the assignment in `MainViewModel.DeviceManagement.cs:152`), #25 (disk-full mid-export → log `FLASHBACK_EXPORT_DISK_FULL` + immediate `DeleteTempFileIfPresent` in `FlashbackExporter.cs`), #27 (`op=` vs `operation=` log-key rename in `CaptureService.cs:611,724`), #29 (90s `AutomationPipeClient` retry-budget cap with `Stopwatch` + new `AutomationPipeRetryBudgetExhaustedException`). Diffs are in the conversation transcript and `.duat/fix-log.md`. Apply manually or pick up in a follow-up `/duat --continue --harden` run.
- **Stop reason:** user paused the loop after observing agent-dispatch cost. Iron Law #5 (no fix without panel) was honored — the in-flight #27 working-tree edit was discarded rather than committed without a panel.

**Open the harden branch and review the commits:**
```
git checkout duat/harden-2026-05-10-1055
git log --oneline performance/optimizations..HEAD
```

**Merge instructions:** open a PR from `duat/harden-2026-05-10-1055` to `performance/optimizations` (where the review originated). Squash-merge or rebase per project convention. Each commit is independently reviewable and revertable.
