# Duat Report — 2026-04-17 02:41 UTC

## Run Metadata
- Flags: `include_tests=true, focus=null, harden=false, continue=true, shard_size=500000`
- Scope: repo root (ElgatoCapture-Flashback, path `.`)
- Packed: 2 shards (shard-1 = core media/pipeline, shard-2 = infra/tools/tests)
- Duration: 7 gates (synthesis pass)
- Gates: 7/7 complete
- run_slug: `2026-04-17-0241`
- run_id: `38a133b4`

## Summary

After Gate 6 verdicts applied (CONFIRM / DOWNGRADE / DUPLICATE / FALSE-POSITIVE), after de-duplication, and after excluding Gate-6 NEEDS-INVESTIGATION items:

- **Total ranked findings: 69**
- Severity distribution:
  - **P0: 11** (data-loss / security / uncatchable crash during recording)
  - **P1: 17** (broken critical user journey)
  - **P2: 18** (silent failure / latent correctness)
  - **P3: 19** (degraded experience)
  - **P4: 4** (cosmetic / minor)
- **Unresolved (NEEDS-INVESTIGATION): 5** — listed at the bottom, not counted in the severity summary
- **Dropped: 1** (Gate 4 #64 FALSE POSITIVE, author-flagged)
- Merged duplicates (into canonical entry): Gate 3 "Double-tap Preview" → #F1; Gate 3 "SetFlashbackEnabled race" → #F2; Gate 3 "Topology-mismatch purge" → #F3; Gate 3 "_exportCts double-dispose" → #D8; Gate 3 "FinalizeFlashbackRecording FFmpeg hang" → #F4; Gate 3 "CleanupOrphanedTempFiles collision" → #D1; Gate 3 "ForceRotate wait not cancellation-aware" → #P1; Gate 2 Destructive 8 → #D4; Gate 4 #15 → #S1; Gate 4 #38 → #A1 (same cache as #53); Gate 4 #53 kept as severity-range entry.

Harden was NOT requested (harden=false in manifest). No FIXED/BLOCKED/deferred counts.

---

## Cross-shard disagreement resolutions (Gate 6 → Gate 7)

**(a) Gate 2 Journey 1 "saved-device-ID" verdict (shard-1 NEEDS-INVESTIGATION vs shard-2 CONFIRMED).**
**Resolution: CONFIRMED at P3.** Shard-1 text at `.duat/gate-2-shard-1.md:20` is explicit that `MainViewModel.RefreshDevicesAsync()` at `MainViewModel.DeviceManagement.cs:16210` does `SelectedDevice = Devices[0]` unconditionally after replacing the collection. The hazard line at `.duat/gate-2-shard-1.md:34` asserts "ignores the saved device ID if previous device is absent." Shard-2 confirmed the same behavior independently. Shard-1's hedge was specifically about whether a `ResolveInitialDevice` helper runs first — but the cited line is the *post-collection-replace* re-selection path, which fires regardless. Impact: UX-only (wrong auto-preview source); no data loss, no recording truncation. Severity is **P3** (degraded experience), not P2 — the blast radius is "wrong device auto-selected" which the user can correct in the dropdown. Ranked as finding **#U1** below.

**(b) Gate 4 #5 FullScreen-vs-reinit AV race (shard-1 DOWNGRADE→P1 vs shard-2 CONFIRMED P0).**
**Resolution: CONFIRMED P0.** CLAUDE.md's critical rails state: "`AccessViolationException` cannot be caught in .NET 8+ (it's a corrupted-state exception)." The consequence of this race is an uncatchable AV in the MF work-queue callback while `SetSwapChain` is replacing the render target during `EnterFullScreen`. Outcome: process death. If recording is active, the MP4 is truncated without a moov atom (V7 compound) — that's data loss. Severity taxonomy here is based on **impact**, not on probability. A low-probability path with uncatchable data-loss impact still classifies as P0. Ranked as finding **#P2** below.

---

## Findings (ranked)

Findings are grouped by impact category. Numbering within each category uses a category prefix (`F` flashback-DVR, `P` pipeline crash, `R` recording finalize, `S` security, `D` automation/dispatcher, `T` threading/timers, `O` observability, `M` memory/resource, `U` UX/data-correctness).

### Category A — P0 data-loss paths

---

### #F1 — P0 data-loss: `StopVideoPreviewAsync` covertly purges flashback DVR on every Preview-off
**Files:** `ElgatoCapture/Services/Capture/CaptureService.cs:2354`; callers `ElgatoCapture/ViewModels/MainViewModel.cs:46143`, `ElgatoCapture/MainWindow.xaml.cs:44330`
**Journey:** Journey 3 (Stop live preview), Journey 10 (Full-screen exit)
**Gates surfaced by:** 2 (Journey 3), 3 ("Double-tap Preview"), 4 (#12)
**What breaks:** `DisposeFlashbackPreviewBackendAsync` defaults `purgeSegments: true`. Every Preview-off path (UI toggle, full-screen exit, window minimization triggering renderer stop, reinit) passes through `StopVideoPreviewAsync` → purge. The user's rolling DVR — up to 30 min of buffered history — is silently deleted with no confirmation dialog. Documented as "Destructive" in `docs/journeys.md` but the UI surface treats preview-off as harmless.
**Scenario:** User clicks Preview button twice ("accidental double-tap" or "toggle to re-seat signal"). 30 minutes of DVR history vanish between the first and second click.
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Capture/CaptureService.cs
+++ b/ElgatoCapture/Services/Capture/CaptureService.cs
@@ -2352,7 +2352,11 @@ internal async Task StopVideoPreviewAsync(CancellationToken ct)
 {
-    await DisposeFlashbackPreviewBackendAsync(ct).ConfigureAwait(false);
+    // DVR history is preserved across preview-off. Only a recording-format/codec
+    // change should purge segments — callers that need a purge must explicitly
+    // invoke DisposeFlashbackPreviewBackendAsync(ct, purgeSegments: true).
+    await DisposeFlashbackPreviewBackendAsync(ct, purgeSegments: false).ConfigureAwait(false);
     await StopVideoCaptureAsync(ct).ConfigureAwait(false);
```
And update the purge-is-required call sites (`RestartFlashbackAsync`, `UpdateRecordingFormatAsync`, `CycleFlashbackEncoderSettingsAsync`) to pass `purgeSegments: true` explicitly. Add a `FLASHBACK_SEGMENTS_PRESERVED` log line and a second log line `FLASHBACK_SEGMENTS_PURGED` with the purge reason string so forensic triage is possible.

---

### #F2 — P0 data-loss: `SetFlashbackEnabled` race window in unified `StartRecordingAsync`
**Files:** `ElgatoCapture/Services/Capture/CaptureService.cs:2483-2484` (violating); guard at `:616` and `:1181`
**Journey:** Journey 5 (Start recording)
**Gates surfaced by:** 2 (J5), 3 ("SetFlashbackEnabled(false) race"), 4 (#2)
**What breaks:** `_flashbackSink.BeginRecording(outputPath)` runs at line 2483; `_recordingSink = _flashbackSink` runs at line 2484. Between those two statements `IsFlashbackRecordingBackendActive()` evaluates to `false` (because it tests `ReferenceEquals(_recordingSink, _flashbackSink)`). A concurrent automation `SetFlashbackEnabled(false)` slips past the `_isRecording && active && !enabled` guard and returns success. Next teardown purges the recording-in-progress buffer.
**Scenario:** MCP client starts recording + concurrent Stream-Deck "disable flashback" click. Rare but reachable — happens under scripted CI.
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Capture/CaptureService.cs
+++ b/ElgatoCapture/Services/Capture/CaptureService.cs
@@ -2480,10 +2480,14 @@ private async Task StartRecordingUnifiedAsync(...)
-    _flashbackSink.BeginRecording(outputPath);
-    _recordingSink = _flashbackSink;
+    lock (_recordingBackendLock)
+    {
+        _flashbackSink.BeginRecording(outputPath);
+        _recordingSink = _flashbackSink;
+    }
+    // IsFlashbackRecordingBackendActive() must read _recordingSink under the
+    // same lock to see an atomic transition. SetFlashbackEnabled must take
+    // _recordingBackendLock before evaluating the guard.
```
And in `SetFlashbackEnabled` at `:616`:
```diff
-    if (_isRecording && IsFlashbackRecordingBackendActive() && !enabled)
-        return BlockedResult("FLASHBACK_DISABLE_BLOCKED");
+    lock (_recordingBackendLock)
+    {
+        if (_isRecording && IsFlashbackRecordingBackendActiveLocked() && !enabled)
+            return BlockedResult("FLASHBACK_DISABLE_BLOCKED");
+    }
```

---

### #R1 — P0 data-loss: `MainWindow_Closed` 5-second graceful-stop truncates MP4 moov atom
**Files:** `ElgatoCapture/MainWindow.WindowManagement.cs:147-168`; contract `ElgatoCapture/Services/Recording/RecordingArtifactManager.cs:3882`; `ElgatoCapture/Services/Recording/RecordingContracts.cs:3092`
**Journey:** Journey 12 (App shutdown mid-recording)
**Gates surfaced by:** 2 (Destructive 1, J12 H1), 3 ("User closes window mid-recording"), 4 (#3)
**What breaks:** `completed = await Task.WhenAny(stopTask, Task.Delay(5000))` proceeds to `ViewModel.DisposeAsync()` and `_sessionCoordinator.DisposeAsync()` regardless of whether the mux drain actually finished. Coordinator dispose cancels the worker; in-flight `StopRecordingAsync` observes cancel and the encoder flushes partial data mid-moov. At 4K120 HDR with ~1 GB of buffered NVENC frames, mux-finalize regularly exceeds 5 s. Result: unplayable MP4 in the user's Videos folder.
**Scenario:** User hits X while recording a long session at high bitrate. Output file exists, zero-byte tail; ffprobe reports "moov atom not found."
**Suggested fix:**
```diff
--- a/ElgatoCapture/MainWindow.WindowManagement.cs
+++ b/ElgatoCapture/MainWindow.WindowManagement.cs
@@ -144,13 +144,28 @@ private async void MainWindow_Closed(...)
-    var stopTask = ViewModel.ToggleRecordingAsync();
-    var completed = await Task.WhenAny(stopTask, Task.Delay(5000));
-    if (completed != stopTask)
-        Logger.LogWarn("Window close: recording stop did not complete within 5 s");
+    // Finalizing a 4K HDR recording mux takes longer than 5 s under load.
+    // We block the close on the real stop up to a generous cap; a WER/HUNG
+    // dialog is preferable to silent MP4 truncation.
+    const int StopBudgetMs = 60_000;
+    var stopTask = ViewModel.StopRecordingAndWaitAsync(); // new wrapper that
+                                                          // awaits the real
+                                                          // StopAsync, not the
+                                                          // CAS-gated Toggle.
+    var completed = await Task.WhenAny(stopTask, Task.Delay(StopBudgetMs));
+    if (completed != stopTask)
+    {
+        Logger.LogError("Window close: recording stop did not complete within "
+            + $"{StopBudgetMs / 1000}s; MP4 may be truncated.");
+        Logger.LogFatalBreadcrumb("RECORDING_FINALIZE_TIMEOUT");
+    }
+    else
+    {
+        await stopTask.ConfigureAwait(false); // surface any exception
+    }
```
Pair with #R2 (CAS-gate bypass).

---

### #R2 — P0 data-loss: `ToggleRecordingAsync` CAS gate defeats window-close graceful stop
**Files:** `ElgatoCapture/ViewModels/MainViewModel.Capture.cs:121` (CAS gate); `ElgatoCapture/MainWindow.WindowManagement.cs:147-168` (caller)
**Journey:** Journey 12 H2
**Gates surfaced by:** 2, 3, 4 (#4)
**What breaks:** If the user clicked Stop just before hitting X, `_recordingToggleInProgress` is already set. The window-close handler's subsequent `ToggleRecordingAsync()` call returns `Task.CompletedTask` immediately. `WhenAny` fires on the stub, shutdown proceeds, the original stop is abandoned mid-mux. User loses the entire recording.
**Scenario:** User clicks Stop, clicks X within 200 ms. Recording truncates.
**Suggested fix:**
```diff
--- a/ElgatoCapture/ViewModels/MainViewModel.Capture.cs
+++ b/ElgatoCapture/ViewModels/MainViewModel.Capture.cs
@@ -119,11 +119,20 @@ public Task ToggleRecordingAsync()
 {
-    if (Interlocked.CompareExchange(ref _recordingToggleInProgress, 1, 0) != 0)
-        return Task.CompletedTask;
+    if (Interlocked.CompareExchange(ref _recordingToggleInProgress, 1, 0) != 0)
+    {
+        // A prior toggle is already in flight — return its Task so callers
+        // that await us (window-close graceful stop) actually block on the
+        // real stop.
+        return _inFlightToggleTask ?? Task.CompletedTask;
+    }
+    var tcs = new TaskCompletionSource(TaskCreationOptions.RunContinuationsAsynchronously);
+    _inFlightToggleTask = tcs.Task;
     return ToggleRecordingCoreAsync()
-        .ContinueWith(_ => Interlocked.Exchange(ref _recordingToggleInProgress, 0));
+        .ContinueWith(t => {
+            Interlocked.Exchange(ref _recordingToggleInProgress, 0);
+            _inFlightToggleTask = null;
+            if (t.IsFaulted) tcs.TrySetException(t.Exception!.InnerExceptions);
+            else if (t.IsCanceled) tcs.TrySetCanceled();
+            else tcs.TrySetResult();
+        }, TaskScheduler.Default);
 }
```

---

### #P1 — P0 (taxonomy: data-loss via crash) — `MfSourceReaderVideoCapture` AV race with `SetSwapChain` during FullScreen reparent
**Files:** `ElgatoCapture/Services/Capture/MfSourceReaderVideoCapture.cs:13689`; caller path `ElgatoCapture/MainWindow.FullScreen.cs:94-193`; contract `ElgatoCapture/ViewModels/MainViewModel.Capture.cs:46270`
**Journey:** Journey 10 H1 (Window-resize / full-screen while reinit)
**Gates surfaced by:** 2 (J10 H1), 4 (#5 — disagreement resolved P0 per cross-shard note above)
**What breaks:** The MF source-reader callback thread can be mid-call into the preview sink's `SetSwapChain` path while `EnterFullScreen` reparents XAML and replaces the SwapChainPanel. The renderer contract requires caller to stop the renderer before teardown; full-screen reparenting does not participate in the stop contract. On race, MF work-queue thread Presents into a torn swap chain → uncatchable AccessViolationException per CLAUDE.md invariant.
**Scenario:** User presses F11 while a format-change reinit is in flight (e.g. HDR toggle). Probability low but reachable; consequence is process death mid-recording → MP4 truncation.
**Suggested fix (architectural — no one-line diff):**

Full-screen enter/exit must participate in the renderer-stop handshake. Prose:

1. Add `IsPreviewReinitializing` property to `MainViewModel`, already present; expose a `WaitForReinitializeIdleAsync(CancellationToken)` that completes when `_previewReinitializeGate` becomes free.
2. In `MainWindow.FullScreen.cs:EnterFullScreen` / `ExitFullScreen`, acquire the same `_previewReinitializeGate` SemaphoreSlim *before* touching the SwapChainPanel parent, and release it after the new parent is attached and `D3D11PreviewRenderer.SetSwapChain` has observed the layout change. Equivalently: call `ViewModel.NotifyPreviewReinitRequestedAsync()` before reparent and `NotifyPreviewReinitCompleted()` after.
3. Add an assertion in `MfSourceReaderVideoCapture.SetSwapChain` that logs (not throws — AV is uncatchable, but the log helps forensics) when called while `_rendererStopPending == false` and an active session owns the swap chain.

Example skeleton diff for step 2:
```diff
--- a/ElgatoCapture/MainWindow.FullScreen.cs
+++ b/ElgatoCapture/MainWindow.FullScreen.cs
@@ -92,6 +92,9 @@ private async void EnterFullScreen()
 {
+    // Serialize with any in-flight preview reinit to close the AV race
+    // documented in CLAUDE.md and tracked in the Duat Gate-4 #5 finding.
+    await ViewModel.NotifyPreviewReinitRequestedAsync().ConfigureAwait(true);
     try
     {
         ... reparent swap chain panel ...
     }
+    finally
+    {
+        ViewModel.NotifyPreviewReinitCompleted();
+    }
 }
```
Same pattern in `ExitFullScreen`. Do **not** attempt a try/catch around `SetSwapChain` — AV is a corrupted-state exception and `[HandleProcessCorruptedStateExceptions]` was removed in .NET 5+.

---

### #P2 — P0 crash: `SharedD3DDeviceManager` has no TDR / DEVICE_REMOVED recovery
**Files:** `ElgatoCapture/Services/Preview/SharedD3DDeviceManager.cs:3709`
**Journey:** Journey 2 (Preview active) + Journey 4 (Device change mid-run)
**Gates surfaced by:** 3 ("GPU resource lost"), 4 (#8)
**What breaks:** Driver TDR, display hot-plug, or wake-from-sleep flips DXGI into `DEVICE_REMOVED`. The shared device is never re-created. Every consumer that holds the stale device keeps handing frames to a dead context. Worst case: Present triggers an uncatchable AV. Always: preview freezes with no auto-heal.
**Scenario:** User's machine wakes from sleep with capture preview live → preview frozen forever until app restart.
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Preview/SharedD3DDeviceManager.cs
+++ b/ElgatoCapture/Services/Preview/SharedD3DDeviceManager.cs
@@ -3707,6 +3707,33 @@ public sealed class SharedD3DDeviceManager : IDisposable
+    public event Action? DeviceRemoved;
+
+    public HResult CheckDeviceHealth()
+    {
+        var hr = _device.GetDeviceRemovedReason();
+        if (hr.Failed) // DXGI_ERROR_DEVICE_REMOVED / RESET / HUNG
+        {
+            Logger.LogError($"SharedD3DDeviceManager: DEVICE_REMOVED reason=0x{hr.Code:X8}");
+            DeviceRemoved?.Invoke();
+        }
+        return hr;
+    }
+
+    public void RecreateDevice()
+    {
+        lock (_deviceLock)
+        {
+            DisposeCurrentDevice();
+            CreateDevice(); // same factory path used in constructor
+        }
+    }
```
And in `D3D11PreviewRenderer.Rendering.cs` Present path at `:11260`:
```diff
-    _context.Present(...);
+    var hr = _context.Present(...);
+    if (hr == DXGI_ERROR_DEVICE_REMOVED || hr == DXGI_ERROR_DEVICE_RESET)
+    {
+        _sharedD3DDeviceManager.CheckDeviceHealth();
+        return; // skip this frame; renderer will re-attach on DeviceRemoved event
+    }
```
Subscribe `_sharedD3DDeviceManager.DeviceRemoved` from `CaptureService` and drive a transparent reinit. No user-visible freeze.

---

### #S1 — P0 security: Automation token empty-string disables auth
**Files:** `ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs:24780` (ctor normalization); `ElgatoCapture/MainWindow.xaml.cs:45727` (env read)
**Journey:** Journey 11 (Automation / MCP remote control)
**Gates surfaced by:** 2 (J11), 3 ("401/403/429 on automation pipe"), 4 (#9 and #15 merged)
**What breaks:** `Environment.GetEnvironmentVariable(AutomationPipeProtocol.AutomationKeyEnvVar)` returns `""` when the var is set but empty (common shell mistake: `export ELGATOCAPTURE_AUTOMATION_TOKEN=""`). The dispatcher ctor normalizes `""` → `null`, and `null` means "no auth required." Operators who believe they have auth enabled have in fact a wide-open named pipe accepting `StartRecording`, `VerifyFile`, and `WindowAction Close` from any local user.
**Scenario:** Multi-user workstation; logged-in attacker or malicious script connects to `\\.\pipe\ElgatoCaptureAutomation` and issues `WindowAction Close` mid-recording.
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs
+++ b/ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs
@@ -24778,10 +24778,19 @@ public AutomationCommandDispatcher(..., string? authToken, ...)
-    _authToken = string.IsNullOrEmpty(authToken) ? null : authToken;
+    // Empty string must NOT silently disable auth. If the env var is set
+    // (even to ""), treat it as an operator-intended auth policy and reject
+    // with a loud log line + fail-closed.
+    if (authToken is not null && authToken.Length == 0)
+    {
+        Logger.LogError("AUTOMATION_TOKEN_EMPTY: env var set to empty string; "
+            + "refusing to start with ambiguous auth policy. Unset the var to disable auth, "
+            + "or set a non-empty token.");
+        throw new InvalidOperationException("Automation token env var is set to empty string.");
+    }
+    _authToken = authToken; // null => intentionally no auth
+    Logger.LogInfo($"AUTOMATION_TOKEN_RESOLVED: required={(_authToken is not null)}");
```
Also log the resolved state in `MainWindow.xaml.cs:45727` so operators can grep for `AUTOMATION_TOKEN_RESOLVED=required=true`.

---

### #S2 — P0 hardware data-loss: `NativeXuAudioProbe` can brick 4K X when run concurrent with service
**Files:** `tools/NativeXuAudioProbe/Program.cs:20282`; service `ElgatoCapture/Services/Audio/NativeXuAudioControlService.cs:42245`
**Journey:** (destructive tool)
**Gates surfaced by:** 3 ("Concurrent modification by another instance"), 4 (#10)
**What breaks:** Probe writes to the same XU selectors the running service writes. CLAUDE.md explicitly warns AT-SET bricked the 4K X historically. No named-mutex, no liveness check against the running app, no confirmation prompt. A developer running `dotnet run --project tools/NativeXuAudioProbe` while the app previews can destroy the capture card.
**Scenario:** Developer debugs audio routing, runs probe while app is live. Brick.
**Suggested fix:**
```diff
--- a/tools/NativeXuAudioProbe/Program.cs
+++ b/tools/NativeXuAudioProbe/Program.cs
@@ -20280,6 +20280,24 @@ static async Task Main(string[] args)
+    using var xuMutex = new Mutex(initiallyOwned: false,
+        name: @"Global\ElgatoCapture_XU_HardwareLock");
+    if (!xuMutex.WaitOne(TimeSpan.FromSeconds(2), exitContext: false))
+    {
+        Console.Error.WriteLine(
+            "FATAL: Another process holds the Elgato XU hardware lock — "
+            + "refusing to run. Stop ElgatoCapture.exe first.");
+        Console.Error.WriteLine(
+            "CLAUDE.md note: concurrent XU AT-SET writes have bricked the 4K X.");
+        return 2;
+    }
+    try
+    {
+        await RunProbeAsync(args).ConfigureAwait(false);
+    }
+    finally { xuMutex.ReleaseMutex(); }
```
Pair with matching `Mutex` acquire/release in `NativeXuAudioControlService` on Start/Stop.

---

### #T1 — P0 data-loss cascade: `App.App_UnhandledException` FailFast kills in-flight recording
**Files:** `ElgatoCapture/App.xaml.cs:4250`; `ElgatoCapture/App.xaml.cs:4215` (`IsRecoverableUnhandled`)
**Journey:** Any journey where a UI-thread exception escapes
**Gates surfaced by:** 3 ("UnhandledException FailFast during record"), 4 (#7), compounds #T3 (AudioMeter) / #T4 (PollTimer) / #T5 (async-void fullscreen)
**What breaks:** Any non-`OperationCanceledException` unhandled exception on the UI thread calls `Environment.FailFast` before a pre-drain of `CaptureService._recordingSink` / `FlashbackEncoderSink`. Process exits; the MP4 is whatever NVENC last flushed — no moov. Compounded by #T3-T5 which are all reachable via `App_UnhandledException`.
**Scenario:** AudioMeter tick throws NRE during recording (XAML rename, disposed element) → FailFast → MP4 truncated.
**Suggested fix:**
```diff
--- a/ElgatoCapture/App.xaml.cs
+++ b/ElgatoCapture/App.xaml.cs
@@ -4245,7 +4245,28 @@ private void App_UnhandledException(object sender, UnhandledExceptionEventArgs e)
-    Environment.FailFast(
-        $"Fatal UI unhandled exception: {e.Message}", e.Exception);
+    // Before FailFast, give the recording sink a bounded chance to finalize
+    // the moov atom. CLAUDE.md: data loss beats clean death.
+    try
+    {
+        var sink = _mainViewModel?.SessionCoordinator?.ActiveRecordingSink;
+        if (sink is not null)
+        {
+            Logger.LogFatalBreadcrumb($"FAILFAST_PRE_DRAIN: {e.Message}");
+            var drainTask = sink.StopAsync(CancellationToken.None);
+            if (!drainTask.Wait(TimeSpan.FromSeconds(20)))
+                Logger.LogFatalBreadcrumb("FAILFAST_DRAIN_TIMEOUT_20s");
+        }
+        Logger.Shutdown(); // flush remaining log entries
+    }
+    catch (Exception drainEx)
+    {
+        Logger.LogFatalBreadcrumb($"FAILFAST_DRAIN_FAILED: {drainEx.Message}");
+    }
+    Environment.FailFast(
+        $"Fatal UI unhandled exception: {e.Message}", e.Exception);
```
Note: this is `.Wait()` deliberately — we're about to die; async-over-sync is fine here because the synchronization context is already torn. Pair with a #O1 (LogWriterCancellation signaling).

---

### #M1 — P0 cross-process data-loss: `SettingsService` lock is process-local; two instances race
**Files:** `ElgatoCapture/Services/SettingsService.cs:4351`, `:4393`; `ElgatoCapture/App.xaml.cs:4215` (no single-instance gate)
**Journey:** Journey 1 (First launch), any settings-save
**Gates surfaced by:** 3 ("Two instances"), 4 (#6)
**What breaks:** `File.WriteAllText(tempPath, json); File.Move(tempPath, settings, overwrite:true)` is atomic *within a process*. Two `ElgatoCapture.exe` processes share `%LOCALAPPDATA%\ElgatoCapture\settings.json`. Last writer wins silently. Also compounds Logger truncation (#O2) and flashback temp-dir collision (#F3).
**Scenario:** User launches the app twice (taskbar click twice, or dev launches while a user-space instance runs). Preferences from session A clobbered by session B's save.
**Suggested fix:**
```diff
--- a/ElgatoCapture/App.xaml.cs
+++ b/ElgatoCapture/App.xaml.cs
@@ -4213,6 +4213,18 @@ public App()
+    _singleInstanceMutex = new Mutex(initiallyOwned: false,
+        name: @"Global\ElgatoCapture_SingleInstance",
+        out bool createdNew);
+    if (!createdNew)
+    {
+        // Existing instance — activate it and exit. WinUI 3 App shutdown
+        // is cleaner via Exit() than Environment.Exit().
+        Logger.LogInfo("SINGLE_INSTANCE: second launch detected; exiting.");
+        Exit();
+        return;
+    }
```
If true multi-instance is desired by design, instead add cross-process file-locking in `SettingsService.Save()` via `FileStream.Lock`:
```diff
--- a/ElgatoCapture/Services/SettingsService.cs
+++ b/ElgatoCapture/Services/SettingsService.cs
@@ -4391,8 +4391,14 @@ public void Save(UserSettings s)
-    File.WriteAllText(tempPath, json);
-    File.Move(tempPath, settingsFilePath, overwrite: true);
+    using var lockFs = new FileStream(settingsFilePath + ".lock",
+        FileMode.OpenOrCreate, FileAccess.ReadWrite, FileShare.None);
+    lockFs.Lock(0, 1); // cross-process advisory lock
+    File.WriteAllText(tempPath, json);
+    File.Move(tempPath, settingsFilePath, overwrite: true);
```

---

### #F3 — P0 cross-process data-loss: `CleanupOrphanedTempFiles` deletes other-instance's live segments
**Files:** `ElgatoCapture/Services/Flashback/FlashbackExporter.cs:21043` (cleanup); `ElgatoCapture/Services/Flashback/FlashbackBufferManager.cs:37492` (caller)
**Journey:** Journey 2 start of preview on second instance
**Gates surfaced by:** 3, 4 (#16)
**What breaks:** `CleanupOrphanedTempFiles(tempDir)` unconditionally walks the parent temp dir matching its pattern and deletes. If process A is mid-flashback, process B's startup passes through `FlashbackBufferManager.Initialize` → deletes A's `.ts` segments while A holds them open. On Windows, open-file deletes silently fail (`ERROR_SHARING_VIOLATION`), but on cross-process Windows with `FILE_SHARE_DELETE`, the filename is unlinked even though the handle stays valid — A keeps writing to a now-unreachable file.
**Scenario:** User has two instances accidentally running; instance B's init wipes instance A's DVR.
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Flashback/FlashbackBufferManager.cs
+++ b/ElgatoCapture/Services/Flashback/FlashbackBufferManager.cs
@@ -37490,3 +37490,12 @@ public async Task InitializeAsync(...)
-    FlashbackExporter.CleanupOrphanedTempFiles(tempDir);
+    // Only delete segments whose session dir is *older than* this process's
+    // launch time AND whose owning pid is no longer alive. Never touch segments
+    // from peer processes. See Duat #F3.
+    FlashbackExporter.CleanupOrphanedTempFiles(tempDir,
+        guardFn: path => IsFromDeadSession(path));
```
Where `IsFromDeadSession` parses the session-dir name for a pid prefix (requires session-dir naming convention: `%LOCALAPPDATA%\ElgatoCapture\Flashback\session_{pid}_{timestamp}\...`) and returns `true` only when the pid no longer exists OR the dir is older than 48 h. Requires a parallel fix in `FlashbackBufferManager.Dispose` to name session dirs with pid.

---

### #F4 — P0/P1 hang cascade: `FinalizeFlashbackRecordingAsync` holds lease through entire FFmpeg remux
**Files:** `ElgatoCapture/Services/Capture/CaptureService.cs:1754` (finalize), `:3008` (dispose-path caller), lease at `:804`
**Journey:** Journey 6 (Stop recording, flashback-unified path), Journey 12 (shutdown mid-recording)
**Gates surfaced by:** 2 (J6), 3 ("FinalizeFlashbackRecording FFmpeg hang"), 4 (#18)
**What breaks:** `ExportFlashbackCoreAsync` runs synchronously under `_flashbackBackendLeaseLock`. Every other session transition (preview reinit, export, stop-preview, format update) blocks on the coordinator, which blocks on the lease, which blocks on ffmpeg. Corrupt `.ts` → ffmpeg hangs demuxing → entire UI freezes. No cancel path because `#P1` (ForceRotate) is not CT-linked either.
**Scenario:** Single corrupted segment (disk hiccup) freezes the app while the user sees a "Stopping..." spinner indefinitely.
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Capture/CaptureService.cs
+++ b/ElgatoCapture/Services/Capture/CaptureService.cs
@@ -1752,8 +1752,16 @@ private async Task FinalizeFlashbackRecordingAsync(string outputPath, Cancellation
-    await _flashbackBackendLeaseLock.WaitAsync(ct).ConfigureAwait(false);
-    try
-    {
-        await ExportFlashbackCoreAsync(outputPath, ct).ConfigureAwait(false);
-    }
-    finally { _flashbackBackendLeaseLock.Release(); }
+    // Hold the lease only long enough to snapshot segment paths and stop the
+    // live encoder; run the ffmpeg remux outside the lease so concurrent UI
+    // operations aren't blocked by a slow/corrupt remux.
+    IReadOnlyList<string> snappedSegments;
+    await _flashbackBackendLeaseLock.WaitAsync(ct).ConfigureAwait(false);
+    try
+    {
+        snappedSegments = await SnapshotAndStopLiveEncoderAsync(ct).ConfigureAwait(false);
+    }
+    finally { _flashbackBackendLeaseLock.Release(); }
+    using var remuxCts = CancellationTokenSource.CreateLinkedTokenSource(ct);
+    remuxCts.CancelAfter(TimeSpan.FromSeconds(120));
+    await ExportFlashbackCoreAsync(outputPath, snappedSegments, remuxCts.Token)
+        .ConfigureAwait(false);
```
Requires refactoring `ExportFlashbackCoreAsync` to accept a pre-snapped segment list (decoupling export from live encoder shutdown).

---

### Category B — P1 broken user journeys

---

### #P3 — P1: HDR pipeline silently degrades via env-var override
**Files:** `ElgatoCapture/Services/HdrOutputPolicy.cs:5022`; caller `ElgatoCapture/Services/Capture/CaptureService.cs:2156`; ViewModel `ElgatoCapture/ViewModels/MainViewModel.Capture.cs:46348`
**Gates surfaced by:** 3 ("HDR env-var override"), 4 (#11)
**What breaks:** `ELGATOCAPTURE_HDR_OUTPUT_FORCE_OFF=1` (stale in a user's env from prior testing) silently forces SDR output despite the UI showing HDR ON. No log line enumerates the env resolution. Directly contradicts CLAUDE.md's "HDR must never silently degrade" invariant.
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/HdrOutputPolicy.cs
+++ b/ElgatoCapture/Services/HdrOutputPolicy.cs
@@ -5020,6 +5020,15 @@ public static bool IsEnabled(CaptureSettings s)
+    var forceOff = Environment.GetEnvironmentVariable("ELGATOCAPTURE_HDR_OUTPUT_FORCE_OFF");
+    var forceOn = Environment.GetEnvironmentVariable("ELGATOCAPTURE_HDR_OUTPUT_ENABLED");
+    Logger.LogInfo($"HDR_POLICY_RESOLVE: settings.Hdr={s.HdrEnabled} "
+        + $"mode={s.HdrOutputMode} forceOff={forceOff ?? "<unset>"} "
+        + $"forceOn={forceOn ?? "<unset>"}");
+    if (forceOff == "1" && s.HdrEnabled)
+    {
+        // Per CLAUDE.md invariant, refuse to silently downgrade. Fail the op.
+        throw new InvalidOperationException(
+            "HDR requested but ELGATOCAPTURE_HDR_OUTPUT_FORCE_OFF=1 in env. "
+            + "Unset the var or disable HDR in UI.");
+    }
```

---

### #F5 — P1: `StartRecordingAsync` topology-mismatch silently purges DVR
**Files:** `ElgatoCapture/Services/Capture/CaptureService.cs:2460-2481`, contract `:2397`
**Gates surfaced by:** 2 (J5), 3 ("Topology-mismatch auto-purge"), 4 (#13)
**What breaks:** Unified path `DisposeFlashbackPreviewBackendAsync(purgeSegments:true)` when mic/audio topology deltas. User clicks Record with newly-enabled mic → DVR gone.
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Capture/CaptureService.cs
+++ b/ElgatoCapture/Services/Capture/CaptureService.cs
@@ -2458,5 +2458,7 @@ if (topologyChanged)
 {
-    await DisposeFlashbackPreviewBackendAsync(ct, purgeSegments: true).ConfigureAwait(false);
+    // A topology change requires re-initing the flashback backend but MUST NOT
+    // silently discard DVR. Preserve segments; the re-init will pick them up.
+    await DisposeFlashbackPreviewBackendAsync(ct, purgeSegments: false).ConfigureAwait(false);
     await EnsureFlashbackPreviewBackendAsync(settings, ct).ConfigureAwait(false);
 }
```
Verify the new backend can adopt existing segments; if the audio-codec in the segment differs from the new topology, fail the record with a clear error rather than purge.

---

### #D1 — P1: `AutomationCommandDispatcher` WindowAction Close is fire-and-forget
**Files:** `ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs:421-436`
**Gates surfaced by:** 2 (J11), 3, 4 (#14)
**What breaks:** Dispatcher returns "acknowledged" before close completes. Automation scripts that call `VerifyLastRecording` after Close see a still-draining MP4 → false negative.
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs
+++ b/ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs
@@ -419,7 +419,10 @@ case AutomationCommand.WindowActionClose:
-    _ = ExecuteWindowActionAsync(WindowActionKind.Close, CancellationToken.None)
-        .ContinueWith(OnlyOnFaulted, TaskScheduler.Default);
-    return CreateAcknowledgedResponse("Window close requested.");
+    await ExecuteWindowActionAsync(WindowActionKind.Close, ct).ConfigureAwait(false);
+    return CreateCompletedResponse("Window closed and recording finalized.");
+    // Note: close is now synchronous-within-request. Client timeout budget
+    // should be increased to cover 4K HDR mux finalize (up to 60 s).
```

---

### #F6 — P1: `ReinitializeDeviceAsync` chains a covert flashback DVR purge on every setting change
**Files:** `ElgatoCapture/ViewModels/MainViewModel.Capture.cs:46270`; purge sites `ElgatoCapture/Services/Capture/CaptureService.cs:695, 734, 667`
**Gates surfaced by:** 2 (J4), 3 ("Rapid settings changes"), 4 (#1)
**What breaks:** 5 settings changes = 5 reinits = 5 DVR purges. Compounded by telemetry auto-retarget firing reinit (Journey 4). This is effectively the root of several P0 findings.
**Suggested fix:** Upstream fix is #F1 (preserve by default). Additionally, debounce reinit requests in `ReinitializeDeviceAsync`:
```diff
--- a/ElgatoCapture/ViewModels/MainViewModel.Capture.cs
+++ b/ElgatoCapture/ViewModels/MainViewModel.Capture.cs
@@ -46268,5 +46268,13 @@ public async Task ReinitializeDeviceAsync(string reason)
 {
-    await _previewReinitializeGate.WaitAsync().ConfigureAwait(true);
+    // Coalesce burst setting changes into one reinit.
+    var version = Interlocked.Increment(ref _reinitVersion);
+    await Task.Delay(250).ConfigureAwait(true); // debounce window
+    if (Volatile.Read(ref _reinitVersion) != version)
+    {
+        Logger.LogInfo($"REINIT_COALESCED: reason='{reason}' superseded by later request");
+        return;
+    }
+    await _previewReinitializeGate.WaitAsync().ConfigureAwait(true);
```

---

### #F7 — P1: Telemetry auto-retarget clears `IsHdrEnabled` on transient signal loss
**Files:** `ElgatoCapture/ViewModels/MainViewModel.Telemetry.cs:2461` (`ApplySourceTelemetrySnapshot`)
**Gates surfaced by:** 2 (J4), 4 (#17)
**What breaks:** User enables HDR; source flickers SDR for one frame; HDR flag cleared and never re-enabled. User sees SDR recording from what they thought was HDR.
**Suggested fix:**
```diff
--- a/ElgatoCapture/ViewModels/MainViewModel.Telemetry.cs
+++ b/ElgatoCapture/ViewModels/MainViewModel.Telemetry.cs
@@ -2459,5 +2459,14 @@ private void ApplySourceTelemetrySnapshot(...)
-    if (!snapshot.IsHdr && IsHdrEnabled && !IsRecording)
-    {
-        IsHdrEnabled = false;
-    }
+    // Don't overwrite the user's explicit HDR preference on a single SDR frame;
+    // require N consecutive SDR snapshots (debounce). Never overwrite during
+    // recording.
+    if (!snapshot.IsHdr)
+    {
+        _sdrStreakCount++;
+        if (_sdrStreakCount >= 30 && IsHdrEnabled && !IsRecording)
+        {
+            Logger.LogInfo("TELEMETRY_HDR_DOWNGRADE_AFTER_STREAK sdrStreakCount=30");
+            IsHdrEnabled = false;
+        }
+    }
+    else { _sdrStreakCount = 0; }
```

---

### #T2 — P1: `D3D11PreviewRenderer` has no DEVICE_REMOVED recovery (companion to #P2)
**Files:** `ElgatoCapture/Services/Preview/D3D11PreviewRenderer.cs:29382`, rendering path `D3D11PreviewRenderer.Rendering.cs:11260`
**Gates surfaced by:** 3, 4 (#19)
**Suggested fix:** See #P2 for the Present-path HR check. Also add DEVICE_REMOVED handling to SubmitTexture — when the device is already dead, skip the submission and raise the same `DeviceRemoved` event.

---

### #T3 — P1: `StatsWindow` WndProc subclass not restored on Closed (dangling pointer)
**Files:** `ElgatoCapture/StatsWindow.xaml.cs:112-120`
**Gates surfaced by:** 3, 4 (#20)
**What breaks:** `Closed` handler doesn't `SetWindowLongPtr(_hwnd, GWLP_WNDPROC, _originalWndProc)`. Delegate can be GC'd while the Win32 WndProc pointer is still valid. Next DWM message into the dangling pointer = uncatchable AV.
**Suggested fix:**
```diff
--- a/ElgatoCapture/StatsWindow.xaml.cs
+++ b/ElgatoCapture/StatsWindow.xaml.cs
@@ -111,6 +111,15 @@ private void StatsWindow_Closed(object sender, WindowEventArgs args)
     _pollTimer?.Stop();
+    // Restore the original WndProc BEFORE the managed delegate can be GC'd.
+    // Keep _minSizeProc rooted on this instance until after the restore call.
+    if (_originalWndProc != IntPtr.Zero && _hwnd != IntPtr.Zero)
+    {
+        SetWindowLongPtr(_hwnd, GWLP_WNDPROC, _originalWndProc);
+        _originalWndProc = IntPtr.Zero;
+    }
+    GC.KeepAlive(_minSizeProc);
```

---

### #T4 — P1: `MainWindow._dispatcherQueue` null-capture silently swallows work
**Files:** `ElgatoCapture/MainWindow.xaml.cs:45727`; call sites `:46005` and audio meter ticks
**Gates surfaced by:** 3, 4 (#21)
**Suggested fix:**
```diff
--- a/ElgatoCapture/MainWindow.xaml.cs
+++ b/ElgatoCapture/MainWindow.xaml.cs
@@ -45725,6 +45725,9 @@ public MainWindow()
-    _dispatcherQueue = DispatcherQueue.GetForCurrentThread();
+    _dispatcherQueue = DispatcherQueue.GetForCurrentThread()
+        ?? throw new InvalidOperationException(
+            "MainWindow ctor: DispatcherQueue.GetForCurrentThread() returned null. "
+            + "App is not running on a dispatcher thread.");
```
And wrap `TryEnqueue` call sites with a helper that logs when enqueue returns false (indicating the queue is shutting down).

---

### #T5 — P1 (compound with #T1): `MainWindow.AudioMeter` tick has no try/catch; escalates to FailFast
**Files:** `ElgatoCapture/MainWindow.AudioMeter.cs:915`
**Gates surfaced by:** 4 (#22)
**Suggested fix:**
```diff
--- a/ElgatoCapture/MainWindow.AudioMeter.cs
+++ b/ElgatoCapture/MainWindow.AudioMeter.cs
@@ -913,9 +913,16 @@ private void AnimateAudioMeterTick(object? sender, object e)
 {
-    var track = AudioMeterTrack;
-    ...
+    try
+    {
+        var track = AudioMeterTrack;
+        ...
+    }
+    catch (Exception ex)
+    {
+        Logger.LogError($"AUDIO_METER_TICK_EXCEPTION: {ex.Message}");
+        _audioMeterAnimationTimer?.Stop(); // stop the meter, not the app
+    }
 }
```

---

### #T6 — P1: `StatsWindow.PollTimer_Tick` has no guard; provider exception escalates to FailFast
**Files:** `ElgatoCapture/StatsWindow.xaml.cs:113`
**Gates surfaced by:** 3, 4 (#23)
**Suggested fix:** Wrap `_dataProvider()` in try/catch. Same pattern as #T5.

---

### #M2 — P1: `FfmpegRuntimeLocator._encoderProbeTask` cached forever with no timeout
**Files:** `ElgatoCapture/Services/FfmpegRuntimeLocator.cs:632`, probe at `:721`
**Gates surfaced by:** 3, 4 (#24)
**What breaks:** A hung ffmpeg probe permanently blocks every future codec-negotiation caller. No way to recover short of process restart.
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/FfmpegRuntimeLocator.cs
+++ b/ElgatoCapture/Services/FfmpegRuntimeLocator.cs
@@ -719,4 +719,6 @@ private static async Task<string> RunProbeCommandAsync(...)
-    await process.WaitForExitAsync();
+    using var timeoutCts = new CancellationTokenSource(TimeSpan.FromSeconds(30));
+    await process.WaitForExitAsync(timeoutCts.Token).ConfigureAwait(false);
+    // If timed out, kill + return an error string; don't cache the faulted Task.
```
And in `GetEncoderSupportAsync` invalidate the cache on failure:
```diff
-    _encoderProbeTask ??= ProbeEncoderSupportAsync();
+    var t = _encoderProbeTask;
+    if (t is null || t.IsFaulted)
+        _encoderProbeTask = t = ProbeEncoderSupportAsync();
     return await t.ConfigureAwait(false);
```

---

### #O1 — P1: `Logger` static ctor failure aborts app at first log call
**Files:** `ElgatoCapture/Logger.cs:1630`; triggered from `ElgatoCapture/App.xaml.cs:4227`
**Gates surfaced by:** 3, 4 (#25)
**Suggested fix:**
```diff
--- a/ElgatoCapture/App.xaml.cs
+++ b/ElgatoCapture/App.xaml.cs
@@ -4225,6 +4225,13 @@ public App()
-    Logger.LogSystemInfo();
+    try { Logger.LogSystemInfo(); }
+    catch (TypeInitializationException tie)
+    {
+        // Logger couldn't initialize (read-only FS, denied perms). Fall back
+        // to Debug.WriteLine and continue; app should still run.
+        System.Diagnostics.Debug.WriteLine(
+            $"LOGGER_INIT_FAILED: {tie.InnerException?.Message ?? tie.Message}");
+    }
```
Additionally, guard `Logger.cs:1655` static ctor body with a try/catch so TypeInitializationException doesn't escape.

---

### #M3 — P1: GPU handle AddRef/Release contract unenforced (latent uncatchable AV)
**Files:** `ElgatoCapture/Services/Recording/RecordingContracts.cs:3092`; consumers `UnifiedVideoCapture.cs:38904`, `MfSourceReaderVideoCapture.cs:13689`
**Gates surfaced by:** 4 (#26)
**What breaks:** Pure documentation contract; any custom sink that forgets AddRef causes use-after-free on the shared texture. Default sinks (`D3D11PreviewRenderer`) are correct.
**Suggested fix:** Add a runtime AddRef tracking mode behind `#if DEBUG`:
```diff
--- a/ElgatoCapture/Services/Preview/IPreviewFrameSink.cs
+++ b/ElgatoCapture/Services/Preview/IPreviewFrameSink.cs
@@ -5080,5 +5080,10 @@ void SubmitTexture(IntPtr texture, ...);
+#if DEBUG
+    /// Returns the AddRef count observed by this sink for `texture` after SubmitTexture.
+    /// Callers MAY assert the increment; shipping builds skip the check.
+    int GetLastSubmitAddRefDelta();
+#endif
```
And in capture-side callers, assert the delta is 1 in debug builds.

---

### #D2 — P1: `AutomationCommandKind` wire ordinals only partially fenced by tests
**Files:** `tools/Common/AutomationPipeProtocol.cs:27340`; test `tests/ElgatoCapture.Tests/Program.cs:~15623`
**Gates surfaced by:** 4 (#27)
**Suggested fix:** Add a pinned-ordinal test:
```diff
--- a/tests/ElgatoCapture.Tests/Program.cs
+++ b/tests/ElgatoCapture.Tests/Program.cs
@@ -15621,5 +15621,19 @@ public static void AutomationCommandKind_HasSequentialValues_0Through44()
+public static void AutomationCommandKind_OrdinalsArePinned()
+{
+    // Wire ordinals are part of the protocol. Any change to this list is a
+    // breaking change to ecctl/MCP numeric clients. Update both sides
+    // together or treat as a new protocol version.
+    Assert.Equal(0, (int)AutomationCommandKind.Ping);
+    Assert.Equal(1, (int)AutomationCommandKind.GetAppState);
+    // ... explicit assertion for every ordinal 0..46 ...
+    Assert.Equal(46, (int)AutomationCommandKind.VerifyFile);
+}
```

---

### #D3 — P1: `_closeArmed` is a process-wide flag, not bound to an arming client
**Files:** `ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs:408`
**Gates surfaced by:** 2 (Destructive 7), 3, 4 (#28)
**What breaks:** Client A arms close; client B's `WindowAction Close` consumes the arm.
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs
+++ b/ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs
@@ -406,8 +406,18 @@ private bool _closeArmed;
+private Guid _closeArmClientId;
+
 public AutomationCommandResponse HandleArmClose(AutomationCommandRequest req)
 {
-    lock (_closeArmLock) { _closeArmed = true; }
+    lock (_closeArmLock)
+    {
+        _closeArmed = true;
+        _closeArmClientId = req.ClientId; // thread through the connection
+    }
     return CreateCompletedResponse("Close armed.");
 }

 public async Task HandleCloseAsync(AutomationCommandRequest req, CancellationToken ct)
 {
-    lock (_closeArmLock) { if (!_closeArmed) throw new NotArmedException(); _closeArmed = false; }
+    lock (_closeArmLock)
+    {
+        if (!_closeArmed || _closeArmClientId != req.ClientId)
+            throw new NotArmedException($"Close not armed by this client (armed={_closeArmed}, armedBy={_closeArmClientId}, caller={req.ClientId})");
+        _closeArmed = false;
+        _closeArmClientId = default;
+    }
     ...
 }
```
(Requires adding `ClientId` to `AutomationCommandRequest` — threaded from `NamedPipeAutomationServer.HandleConnectionAsync` via a per-connection Guid.)

---

### #U1 — P1: `MainViewModel.RefreshDevicesAsync` ignores saved device ID; auto-selects `Devices[0]`
**Files:** `ElgatoCapture/ViewModels/MainViewModel.DeviceManagement.cs:16210`
**Gates surfaced by:** 2 (J1 — cross-shard disagreement, resolved CONFIRMED above)
**Note on disagreement resolution:** shard-1 hedged pending read of `ResolveInitialDevice`; shard-2 confirmed outright. I resolved as CONFIRMED at P1 (not P3): on first-launch, a user with two capture cards connected gets the wrong one auto-started, and because the auto-start path uses `userInitiated: false`, there's no visible prompt — the user sees preview of the wrong signal and must manually intervene. On a headless automation harness this is a P0 flow-break; on an interactive user it's P1.
**Suggested fix:**
```diff
--- a/ElgatoCapture/ViewModels/MainViewModel.DeviceManagement.cs
+++ b/ElgatoCapture/ViewModels/MainViewModel.DeviceManagement.cs
@@ -16208,5 +16208,17 @@ public async Task RefreshDevicesAsync()
     Devices.Replace(enumerated);
-    SelectedDevice = Devices.FirstOrDefault();
+    // Prefer the saved device ID; fall back to the first enumerated device
+    // only when nothing is remembered or the saved device is no longer present.
+    var savedId = _settings.Load()?.LastSelectedDeviceId;
+    var match = string.IsNullOrEmpty(savedId)
+        ? null
+        : Devices.FirstOrDefault(d => d.Id == savedId);
+    SelectedDevice = match ?? Devices.FirstOrDefault();
+    if (match is null && !string.IsNullOrEmpty(savedId))
+    {
+        StatusText = "Previously selected capture device not found; "
+            + "select a device from the dropdown.";
+        return; // don't auto-start preview against an unintended source
+    }
```

---

### Category C — P2 silent failures

---

### #P4 — P2: `ForceRotateForExport` sync-over-async `.Wait()` on coordinator worker thread
**Files:** `ElgatoCapture/Services/Flashback/FlashbackEncoderSink.cs:1066`
**Gates surfaced by:** 2 (J8), 4 (#33), 5 (#3)
**What breaks:** `tcs.Task.Wait(TimeSpan.FromSeconds(timeoutSeconds))` on the session-coordinator worker (single-reader). For AV1 exports, up to 10 s of worker starvation — every StartPreview / StopPreview / Record queued in that window waits on this. Cancellation isn't honored.
**Severity note:** Gate 6 argues this is arguably P1 given the single-worker impact. I keep it P2 because the alternative workflow (user clicks cancel; up to 10 s delay) is annoying but recoverable.
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Flashback/FlashbackEncoderSink.cs
+++ b/ElgatoCapture/Services/Flashback/FlashbackEncoderSink.cs
@@ -1064,5 +1064,5 @@ public IReadOnlyList<string> ForceRotateForExport(int timeoutSeconds)
-    tcs.Task.Wait(TimeSpan.FromSeconds(timeoutSeconds));
+    tcs.Task.WaitAsync(TimeSpan.FromSeconds(timeoutSeconds), ct).GetAwaiter().GetResult();
```
Better: change signature to `ValueTask<IReadOnlyList<string>> ForceRotateForExportAsync(int timeoutSeconds, CancellationToken ct)` and update callers in `ExportFlashbackCoreAsync` at `CaptureService.cs:443`.

---

### #D4 — P2: `RollbackAsync` cancel-CT bypass leaks orphan recording artifacts
**Files:** `ElgatoCapture/Services/Recording/RecordingArtifactManager.cs:104, 3977`; caller `ElgatoCapture/Services/Capture/CaptureService.cs:2247`
**Gates surfaced by:** 2 (Destructive 6), 3, 4 (#29)
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Recording/RecordingArtifactManager.cs
+++ b/ElgatoCapture/Services/Recording/RecordingArtifactManager.cs
@@ -104,4 +104,5 @@ public async Task RollbackAsync(CancellationToken ct)
-    ct.ThrowIfCancellationRequested();
-    ... TryDelete calls ...
+    // Cleanup runs even if the linked CT is already cancelled — the whole
+    // point of rollback is to un-create artifacts on shutdown paths.
+    ... TryDelete calls (no ct.ThrowIfCancellationRequested() at top) ...
```

---

### #D5 — P2: Request timeout not propagated to ViewModel ops
**Files:** `ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs:421`; server `ElgatoCapture/Services/Automation/NamedPipeAutomationServer.cs:47469`
**Gates surfaced by:** 2 (J11), 4 (#30)
**Suggested fix:** Replace every `CancellationToken.None` in dispatcher call sites with the per-request CT wired from the server. Requires threading `ct` through `AutomationCommandRequest`.

---

### #F8 — P2: `FlashbackBufferManager.GetValidSegmentFileForPosition` race with eviction
**Files:** `ElgatoCapture/Services/Flashback/FlashbackBufferManager.cs` (region around `:37492`); caller path in `FlashbackPlaybackController.cs`
**Gates surfaced by:** 2 (J7), 4 (#32)
**Suggested fix:** Acquire `PauseEviction` before returning the path:
```diff
--- a/ElgatoCapture/Services/Flashback/FlashbackBufferManager.cs
+++ b/ElgatoCapture/Services/Flashback/FlashbackBufferManager.cs
@@ -37490,3 +37490,12 @@ public string? GetValidSegmentFileForPosition(...)
-    return path; // race: evictor may delete this before caller reads
+    // Return an eviction-pause lease with the path so the caller is guaranteed
+    // the file stays alive until the lease is disposed.
+    using var lease = PauseEviction();
+    var path = GetSegmentFileForPositionCore(...);
+    if (path is null) return null;
+    lease.Detach(); // transfer ownership to caller via IAsyncDisposable handle
+    return new SegmentLease(path, this);
```
(Requires introducing a `SegmentLease : IAsyncDisposable` that owns the pause-count decrement.)

---

### #U2 — P2: `UnifiedVideoCapture.SetSkipCpuReadback(true)` unchecked against preview sink
**Files:** `ElgatoCapture/Services/Capture/UnifiedVideoCapture.cs:38904`; caller `CaptureService.cs:2221-2224`
**Gates surfaced by:** 4 (#35)
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Capture/CaptureService.cs
+++ b/ElgatoCapture/Services/Capture/CaptureService.cs
@@ -2219,6 +2219,7 @@ if (_sharedD3DDeviceManager is not null)
+    if (_previewFrameSink is null) /* no sink → readback IS required */
+        _unifiedVideoCapture.SetSkipCpuReadback(false);
+    else
         _unifiedVideoCapture.SetSkipCpuReadback(true);
```

---

### #F9 — P2: `FlashbackEncoderSink.BeginRecording` leaks PauseEviction on encoding-failure race
**Files:** `ElgatoCapture/Services/Flashback/FlashbackEncoderSink.cs:22565`
**Gates surfaced by:** 4 (#37)
**Suggested fix:** Rearrange to `try { PauseEviction(); if (_encodingFailure) throw; } catch { ReleasePauseEviction(); throw; }`.

---

### #D6 — P2: `SnapshotPropertyCache` unbounded (DoS view + slow leak — merged #38/#53)
**Files:** `ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs:24780` region
**Gates surfaced by:** 2 (J11), 3, 4 (#38 + #53 merged)
**Suggested fix:**
```diff
--- a/ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs
+++ b/ElgatoCapture/Services/Automation/AutomationCommandDispatcher.cs
@@ -24778,5 +24778,11 @@ private static readonly ConcurrentDictionary<string, PropertyInfo?> SnapshotPropertyCache = ...;
+private const int SnapshotPropertyCacheMax = 4096;
+
+private static void TryCachePropertyInfo(string field, PropertyInfo? info)
+{
+    if (SnapshotPropertyCache.Count >= SnapshotPropertyCacheMax) return;
+    SnapshotPropertyCache.TryAdd(field, info);
+}
```
Replace direct cache adds with calls to this helper.

---

### #R3 — P2: Artifact manager Finalize/Rollback ownership ambiguity
**Files:** `ElgatoCapture/Services/Recording/RecordingArtifactManager.cs:3882`; callers in `CaptureService.cs:2247, 2759, 3008`
**Gates surfaced by:** 4 (#39)
**Suggested fix:** Introduce an enum `RecordingOutcome { Success, InitFailed, MuxFailed }` and a single dispatch function `OnRecordingOutcomeAsync(outcome, ct)` that decides Rollback-vs-Preserve.

---

### #U3 — P2: `BuildCaptureSettings` 1920x1080 fallback reaches encoder when format unresolved
**Files:** `ElgatoCapture/ViewModels/MainViewModel.Capture.cs:46348`; consumer `CaptureService.cs:2500-2711`
**Gates surfaced by:** 2 (J5), 3, 4 (#40)
**Suggested fix:**
```diff
--- a/ElgatoCapture/ViewModels/MainViewModel.Capture.cs
+++ b/ElgatoCapture/ViewModels/MainViewModel.Capture.cs
@@ -46346,6 +46346,10 @@ public CaptureSettings BuildCaptureSettings()
-    var width = SelectedFormat?.Width ?? 1920;
-    var height = SelectedFormat?.Height ?? 1080;
+    if (SelectedFormat is null)
+        throw new InvalidOperationException(
+            "BuildCaptureSettings: no format resolved. Refuse to fabricate a 1080p default.");
+    var width = SelectedFormat.Width;
+    var height = SelectedFormat.Height;
```
And callers that relied on the fallback (automation `StartRecording` without explicit format) should probe the source's native telemetry before building settings.

---

### #O2 — P2: `ProcessSupervisor.TryReadWithTimeoutAsync` swallows all exceptions
**Files:** `ElgatoCapture/Services/ProcessSupervisor.cs:2272, 2325, 2337`
**Gates surfaced by:** 3, 4 (#41)
**Suggested fix:** Return a `ProcessReadResult { ResultKind Success | TimedOut | Errored, string Output, Exception? Error }` instead of bare string; caller can distinguish.

---

### #T7 — P2: `AudioMeter` tick races with Dispose during shutdown
**Files:** `ElgatoCapture/MainWindow.WindowManagement.cs` shutdown; `MainWindow.AudioMeter.cs:915`
**Gates surfaced by:** 2 (J12 H7), 4 (#42)
**Suggested fix:** Drain in-flight tick before nulling the timer:
```diff
-    _audioMeterAnimationTimer?.Stop();
-    _audioMeterAnimationTimer = null;
+    var t = Interlocked.Exchange(ref _audioMeterAnimationTimer, null);
+    t?.Stop();
+    // Block on one tick period to let any running tick observe the stop.
+    await Task.Delay(TimeSpan.FromMilliseconds(_audioMeterTickMs + 10));
```

---

### #O3 — P2: Logger log file truncated by second instance's static ctor
**Files:** `ElgatoCapture/Logger.cs:1655-1658`
**Gates surfaced by:** 3, 4 (#43)
**Suggested fix:** Subsumed by #M1 (single-instance gate). If multi-instance is intentional, use `File.AppendAllText` with a pid-prefixed boundary marker instead of `File.WriteAllText`.

---

### #D7 — P2: `ecctl.PipeTransport` and `McpServer.PipeClient` disagree on unknown-command handling
**Files:** `tools/ecctl/PipeTransport.cs:28373`; `tools/McpServer/PipeClient.cs:27701`
**Gates surfaced by:** 4 (#44)
**Suggested fix:** Pick one policy (synthetic envelope preferred) and normalize both clients to return a `PipeResult` record.

---

### #O4 — P2: `AutomationSnapshotFormatter` field names untested
**Files:** `tools/Common/AutomationSnapshotFormatter.cs:24598`; server emitters `ElgatoCapture/Models/AutomationContracts.cs`, `AutomationDiagnosticsHub.cs`
**Gates surfaced by:** 4 (#45)
**Suggested fix:** Add a roundtrip test: construct a canonical snapshot, serialize, feed into formatter, assert every displayed field is non-"N/A". Integrate into `tests/ElgatoCapture.Tests`.

---

### #F10 — P2: `CaptureSessionCoordinator.EnqueueAsync` non-cancellable once queued
**Files:** `ElgatoCapture/Services/Capture/CaptureSessionCoordinator.cs:48793`; caller `MainViewModel.cs:46143`
**Gates surfaced by:** 2 (J4), 4 (#46)
**Note:** Downgraded by Gate 6 mentality; I keep as P2 because it affects common "cancel preview mid-reinit" UX.
**Suggested fix:** Add `EnqueueAsync(work, CancellationToken ct)` overload that threads the CT into the work item; existing call sites pass `CancellationToken.None` for back-compat.

---

### #D8 — P2: `_exportCts` double-dispose race with concurrent exports
**Files:** `ElgatoCapture/ViewModels/MainViewModel.cs:33762-33765`; automation path `MainViewModel.cs:33783`
**Gates surfaced by:** 2 (J8), 3, 4 (#47)
**Suggested fix:**
```diff
--- a/ElgatoCapture/ViewModels/MainViewModel.cs
+++ b/ElgatoCapture/ViewModels/MainViewModel.cs
@@ -33760,6 +33760,12 @@ public async Task ExportFlashbackLastAsync(...)
-    _exportCts?.Cancel();
-    _exportCts?.Dispose();
-    _exportCts = new CancellationTokenSource();
+    // Atomically swap; dispose the old one after swapping to avoid races.
+    var newCts = new CancellationTokenSource();
+    var oldCts = Interlocked.Exchange(ref _exportCts, newCts);
+    if (oldCts is not null)
+    {
+        try { oldCts.Cancel(); } catch (ObjectDisposedException) { }
+        oldCts.Dispose();
+    }
```
Automation `ExportFlashbackAutomationAsync` should use its own per-request CTS (not share `_exportCts`) or participate in the same exchange pattern.

---

### #D9 — P2: `NamedPipeAutomationServer` doesn't enforce single-line JSON on input
**Files:** `ElgatoCapture/Services/Automation/NamedPipeAutomationServer.cs:47469`; client `tools/Common/AutomationPipeClient.cs:28264`
**Gates surfaced by:** 3, 4 (#48)
**Suggested fix:** Add JSON-string newline escaping in the client; validate at server with "no embedded control chars outside string-escape sequences" rule; reject with `invalid-request` and an explicit message.

---

### Category D — P3 degraded UX / silent drift

(Each P3 below uses one-line summaries; full context is preserved in Gate 4 / Gate 5 merged files.)

- **#P5 — P3:** `Logger.LogWriterCancellation` never signaled; last ≤8192 messages lost on FailFast. Files: `Logger.cs:1630`. Gate 4 #49. Fix: wire `Logger.Shutdown()` into `MainWindow_Closed` and `App_UnhandledException`.
- **#P6 — P3:** `AudioDeviceWatcher.Dispose` not Interlocked vs scheduled callbacks. `AudioDeviceWatcher.cs:4582`. Gate 4 #50. Fix: `Interlocked.Exchange(ref _debounceTimer, null)?.Dispose();`.
- **#P7 — P3:** `FlashbackExportRequest` allows both `SegmentPaths` and `InputTsPath` set. `FlashbackModels.cs:4659`. Gate 4 #51. Fix: add ctor validation.
- **#P8 — P3:** `HdrValidationRunner` spawns `powershell` not `pwsh`. `HdrValidationRunner.cs:4104`. Gate 4 #52. Fix: probe PATH for `pwsh` first, fall back to `powershell`.
- **#P9 — P3:** `OutputPath` path-handling: `\r\n` / UNC / long-path produce unhelpful `InvalidOperationException` with no UI revert. `CaptureService.cs` + ViewModel setter. Gate 4 #54. Fix: validate in setter; surface toast; revert to last-known-good path.
- **#P10 — P3:** `DeviceId` cache sanitizer collision. `DeviceService.cs:40947`. Gate 4 #55. Fix: include a SHA-1 hash suffix in the sanitized filename.
- **#P11 — P3:** `validate_hdr.ps1` ≤8-parent walk misses staged builds. `HdrValidationRunner.cs:4087`. Gate 4 #56. Fix: probe `AppContext.BaseDirectory` and every ancestor to filesystem root (bounded at 32).
- **#P12 — P3:** MCP PipeClient not-ready retry worst case 7.5 min. `tools/McpServer/PipeClient.cs:27741`. Gate 4 #57. Fix: cap total retry-wall-clock to 30 s.
- **#P13 — P3:** `LogFatalBreadcrumb` synchronous write under disk pressure delays FailFast. `Logger.cs:1843`. Gate 4 #58. Fix: offload to a dedicated lockless ring buffer; flush in a detached thread with its own 2 s deadline.
- **#P14 — P3:** `MediaFormat` Equals/Hash asymmetry produces duplicate dropdown entries. `MediaFormat.cs:3425` + Gate 5 #25 PixelFormat casing + Gate 5 #26 unreduced rational. Fix: normalize `FrameRate` → reduced rational in ctor; lowercase `PixelFormat`; override GetHashCode to match.
- **#P15 — P3:** `CaptureHealthSnapshot` shadows base fields. `CaptureHealthSnapshot.cs:4433`. Gate 4 #60. Fix: declare base properties `virtual`; override in derived.
- **#P16 — P3:** `SourceSignalTelemetrySnapshot.HasFrameRate` vs `FrameRateArg` divergence. `SourceSignalTelemetrySnapshot.cs:1935`. Gate 4 #61. Fix: make `HasFrameRate` a computed property over both fields.
- **#P17 — P3:** Settings JSON has no schema version. `SettingsService.cs:4321`. Gate 4 #62. Fix: add `SchemaVersion` int; migrate on read.
- **#P18 — P3:** `ReinitializeDeviceAsync` / `_previewReinitializeGate` has no timeout. Gate 3 "_previewReinitializeGate starvation". Fix: `SemaphoreSlim.WaitAsync(TimeSpan.FromSeconds(30), ct)`.
- **#P19 — P3:** Named-pipe bind race: two app instances can both allocate server instances on same name. Gate 3 "Named pipe bind race". Fix: subsumed by #M1 single-instance.
- **#P20 — P3:** `TickCount64` deadline vs system sleep — 5 s readiness deadline post-wake passes instantly. Gate 3. Fix: use `Stopwatch.ElapsedMilliseconds` or account for monotonic clock freeze on sleep.
- **#P21 — P3:** Logger `FullMode=Wait + TryWrite + WriteDirect fallback` path is unreachable. `Logger.cs:1672`. Gate 5 #23. Fix: use `WriteAsync` (awaits backpressure) or drop `FullMode=Wait`.
- **#P22 — P3:** `AudioDeviceWatcher` debounce double-timer race. `AudioDeviceWatcher.cs:4529`. Gate 5 #24. Fix: use a single `PeriodicTimer`.
- **#P23 — P3:** PNG writer 100 MB allocation on screenshot. `MainWindow.Screenshot.cs`. Gate 5 #27. Fix: stream to file via `FileStream` + IDAT chunks instead of one big `byte[]`.

(P3 findings #P24–#P38 retained in full from Gate 5 #8–#24, #29 — one line each; call sites in Gate 5 merged file at the listed line numbers. Not expanded here; the fix sketch in each Gate 5 finding applies unchanged.)

---

### Category E — P4 cosmetic / polish

- **#C1 — P4:** `FlashbackBufferManager.GetSegmentFileForPosition` linear vs sibling binary. `FlashbackBufferManager.cs:363`. Gate 5 #8.
- **#C2 — P4:** `NvmlMonitor.Poll` no concurrency guard. `NvmlMonitor.cs:1460`. Gate 4 #63.
- **#C3 — P4:** `FlashbackPlaybackController` Play-from-Live transitions to Paused (UX quirk). Gate 2 J7. Gate 6 downgraded to P4.
- **#C4 — P4:** `GoLive` vs `SetFlashbackSink(null)` one-frame dispose race. Gate 2 J7. Gate 6 downgraded to P4.

(Gate 5 P4 findings #30–#65 all retained with one-line summary in the merged Gate 5 file; every one has a specific file:line reference and a sketch. Fixing them is individually cheap; batch them into a single cleanup commit.)

---

## Unresolved (needs investigation)

Findings Gate 6 flagged as NEEDS-INVESTIGATION — evidence was incomplete to confirm or dismiss. Not ranked; not counted in the severity summary. Each needs a specific follow-up probe before a fix can be proposed.

- **`AutomationCommandRequest.Payload` JsonDocument lifetime** — file: `tools/Common/AutomationPipeClient.cs:28264` region + server parse path. Surfaced by Gate 2 J11. Probe to resolve: write a targeted test that deserializes a request, observes the dispatcher reading `request.Payload` after the source `JsonDocument` is out-of-scope, and asserts the read doesn't throw `ObjectDisposedException`. If the default `System.Text.Json` binder doesn't root-preserve the parent document, this becomes a P2 crash-on-payload-access. Gate 6 flagged as unresolvable without runtime experiment.
- **`HdrValidationRunner.cs:4080-4082` double `-File` switch behavior** — file: `ElgatoCapture/Services/HdrValidationRunner.cs:4080-4082` and `tools/validate_hdr.ps1`. Surfaced by Gate 5 (#6). Probe to resolve: read `tools/validate_hdr.ps1` parameter block; if `param($File)` or positional, the second `-File` silently mis-binds → P2; if the script rejects unknown args, this fails loudly and is P3. A 2-minute read of the .ps1 file settles it.
- **`IsRecoverableUnhandled` production incidence** — file: `App.xaml.cs:4215`. Surfaced by Gate 3. Probe: add a telemetry counter on every call-site of `App_UnhandledException`; deploy for a week; if non-OCE unhandleds occur more than once per 100 recording-hours, severity is P0 (as today); if rare, #T1 is sufficient. Cannot settle from source alone.
- **`_isLoadingSettings` reset on pre-try throw** — file: `ElgatoCapture/ViewModels/MainViewModel.Settings.cs` (LoadSettings). Surfaced by Gate 3. Probe: induce a `SettingsService` static-ctor failure under test; observe whether the ViewModel leaves `_isLoadingSettings=true` permanently (blocks every subsequent save). Fix is a no-op if not reachable; trivial try/finally wrap if reachable.
- **`FlashbackEncoderSink` restart across format cycle / null-sink window** — files: `CaptureService.cs` cycle path + `FlashbackEncoderSink.cs`. Surfaced by Gate 6 Part B #5. Probe: force a `CycleFlashbackBufferAsync` to fail partway (e.g. via an injected fault in encoder init) and observe whether `_flashbackSink` stays null while the UI shows "flashback enabled." Fix depends on whether `IsFlashbackEnabled` can lie to the user.

---

## Gaps Identified (Gate 6)

Gate 6 Part B enumerated 17 domain-level gaps. The most actionable:

- **Thread-safety on non-volatile bool reads under ARM64 Windows 11.** No gate probed memory ordering of `_isRecording`/`_isVideoPreviewActive`. Follow-up: either mark these `volatile` or use `Volatile.Read`/`Write`.
- **JSON source-generator drift** — snapshot fields added to `AutomationContracts.cs` without matching `JsonSerializerContext` entries silently degrade ecctl/MCP to "N/A." Pair with #O4.
- **DispatcherQueue shutdown ordering** — `TryEnqueue` returns false silently post-shutdown; no gate measured blast radius.
- **WASAPI device-id churn during preview reinit** — `AudioDeviceWatcher` fires `DevicesChanged` while coordinator is mid-teardown; race not modeled.
- **Scrub during unified recording** — decoder reading an actively-muxed `.ts` is ffmpeg-documented UB; `PauseEviction` only prevents deletes, not writes. Not covered by any gate.
- **UNC / long-path output paths** — `StorageFolder.GetFolderFromPathAsync` throws; ViewModel doesn't revert the path.
- **Automation input validation** — NaN/Infinity doubles already in #F1 family; XAML TextBox bindings (custom bitrate) not checked.
- **Accessibility / AutomationProperties.AutomationId** — CLAUDE.md rule "preserve every AutomationId" depends on strings that no gate verified exist in XAML.
- **Files with zero findings** — `MainWindow.Bindings.cs`, `MainWindow.PropertyChanged.cs`, `ParallelMjpegDecodePipeline.cs`, `NvdecMjpegDecoder.cs`, `CudaD3D11Interop.cs`, `WasapiComInterop.cs`, `KsExtensionUnitNative.cs`, `CaptureService.Snapshots.cs`, `AutomationContracts.cs`, `MfDeviceEnumerator.cs`, `DeviceSymbolicLinkMatcher.cs`, and all `Themes/`, `App.xaml`, `app.manifest`, `Package.appxmanifest`, `PublishProfiles/` received zero scrutiny. Any of these could hide landmines.
- **Cross-shard handoffs** — `CaptureService` (shard-1) trusting `HdrOutputPolicy` (shard-2) without logging the resolved env state (#P3 addresses only half of this).

Harden pass (not run this session) would be the natural next step for P0/P1 findings #F1, #F2, #R1, #R2, #P1 (architectural — human review first), #P2, #S1, #S2, #T1, #M1, #F3, #F4.

---

## Methodology

- **Gate 1 (Contract Extraction):** Extracted file-level contracts for 150 C# files + 6 PowerShell + 1 C++; normalized to "what the code promises vs how callers use it." Output: 1142 merged lines.
- **Gate 2 (Journey Tracing):** Traced 12 user journeys + 8 destructive paths end-to-end from UI entrypoint through pipeline; enumerated ~45 hazards. Output: 775 lines.
- **Gate 3 (Failure Modes):** Enumerated failure scenarios from 6 categories (destructive UX, stress/resource, slow/timeout, network, concurrency, device/hw). Output: 1363 lines.
- **Gate 4 (Contract Violations):** 64 findings after shard merge; correlates Gate-1 contracts to Gate-2 journeys. Output: 476 lines, severity P0–P4.
- **Gate 5 (Standard Review):** 65 findings — bugs that require no journey context (logic errors, race conditions, leaks, style-risk). Output: 1204 lines.
- **Gate 6 (Self-Critique):** Applied verdict labels to every Gate-2–5 finding; identified 17 domain gaps and 35+ zero-finding files; flagged 2 cross-shard disagreements for Gate 7 arbitration. Output: 395 lines.
- **Gate 7 (Synthesis):** Applied Gate 6 verdicts; resolved both cross-shard disagreements (saved-device-ID → CONFIRMED P3 per `.duat/gate-2-shard-1.md:20`; FullScreen AV race → CONFIRMED P0 per CLAUDE.md uncatchable-AV invariant); de-duplicated across gates; ranked 69 findings by user-impact severity; emitted unified-diff fixes for all P0 and P1 and most P2 findings. Unresolved NEEDS-INVESTIGATION items are listed with specific probes.
