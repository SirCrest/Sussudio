namespace Sussudio;

// XAML-facing preview renderer adapter family. PreviewRendererHostController
// owns the D3D renderer, CPU fallback source, frame counters, and public/reinit
// disposal path; MainWindow supplies UI elements and startup side-effect adapters.
public sealed partial class MainWindow
{
}
