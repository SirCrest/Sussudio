namespace Sussudio;

// XAML-facing Flashback adapter family. Focused controllers own command
// semantics, settings sync, timeline visibility, polling, scrub state,
// playhead motion, marker/playback presentation, and export progress. The
// sibling MainWindow.Flashback.* partials keep XAML event names stable while
// keeping each adapter surface easy to inspect.
public sealed partial class MainWindow
{
}
