namespace Sussudio;

// XAML-facing preview transition adapter family. Focused controllers own button
// actions, audio fades, delayed fade-in, reinit transitions, startup overlay,
// and preview content/shell animations; sibling MainWindow.PreviewTransitions.*
// partials keep each callback surface grouped by runtime concern.
public sealed partial class MainWindow
{
}