namespace Sussudio;

// XAML-facing full-screen adapter family. FullScreenController owns transition
// state, overlay mechanics, and key routing; Flashback controllers own command
// execution and scrub state. These partials keep MainWindow event names stable.
public sealed partial class MainWindow
{
}
